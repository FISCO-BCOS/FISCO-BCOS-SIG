# 自定义任务：FISCO BCOS 常见问题 FAQ 整理与翻译

## 任务描述

**任务类型**：文档贡献

**任务难度**：2 星

**任务内容**：整理 FISCO BCOS 社区中常见的技术问题，编写一份《FISCO BCOS 新手常见问题 FAQ》文档，帮助新人快速解决入门过程中遇到的常见障碍。

**产出物**：一份结构化的 FAQ 文档（Markdown 格式），可直接提交至 FISCO-BCOS-DOC 仓库。

## FAQ 文档

---

# FISCO BCOS 新手常见问题 FAQ

## 一、部署相关

### Q1：build_chain.sh 下载失败怎么办？

**A**：官方提供了 Gitee 备用地址：

```bash
curl -#LO https://gitee.com/FISCO-BCOS/FISCO-BCOS/releases/download/v3.11.0/build_chain.sh
```

### Q2：端口被占用如何解决？

**A**：使用 `-p` 参数指定自定义起始端口：

```bash
bash build_chain.sh -l 127.0.0.1:4 -p 40300,40200
```

其中 `40300` 是 P2P 端口起始值，`40200` 是 RPC 端口起始值。

### Q3：节点启动失败，日志显示"cert error"？

**A**：通常是因为 SDK 证书未正确拷贝。确保：

```bash
cp -r nodes/127.0.0.1/sdk/* console/conf/
```

如果使用国密模式，证书文件名会带有 `sm_` 前缀，注意区分。

### Q4：启动节点时提示"permission denied"？

**A**：确保 `fisco-bcos` 二进制文件有执行权限：

```bash
chmod +x nodes/127.0.0.1/node0/fisco-bcos
```

## 二、合约开发相关

### Q5：Solidity 合约编译失败？

**A**：FISCO BCOS 3.x 推荐使用 Solidity >= 0.6.0 版本。旧版合约（< 0.6.0）可能需要手动适配。注意：

- `pragma solidity ^0.6.0;` 是可以的
- `pragma solidity >=0.4.24 <0.6.11;` 在 3.x 中可能不兼容

推荐的编译工具：[FISCO BCOS 控制台](https://fisco-bcos-documentation.readthedocs.io/zh_CN/latest/docs/console/console.html) 或 Remix IDE。

### Q6：部署合约提示"group not exist"？

**A**：检查群组名是否正确。默认群组名为 `group0`。查看当前所有群组：

```bash
# 在控制台中
[group0]: /apps> getGroupList
```

### Q7：交易一直不被打包确认？

**A**：可能的原因：
1. 发送交易时未指定正确的群组
2. BlockLimit 设置过小，交易在被打包前已过期
3. 共识节点未正常运行（检查进程状态）
4. 请求的节点不是共识节点（检查 `getSealerList`）

## 三、SDK 使用相关

### Q8：C++ SDK 编译失败？

**A**：确保满足以下条件：
- CMake >= 3.14
- GCC >= 7.0 或 Clang >= 5.0
- Boost >= 1.69

常见编译错误和解决方法：

```bash
# 如果提示找不到 openssl
sudo apt install libssl-dev

# 如果 vcpkg 下载依赖失败（国内网络问题）
export X_VCPKG_ASSET_SOURCES=x-azurl,http://106.15.181.5/
```

### Q9：Java SDK 连接失败"handshake failed"？

**A**：可能出现的原因和解决方法：

1. **证书不匹配**：确保使用的是当前链的 SDK 证书（位于 `nodes/127.0.0.1/sdk/` 目录）
2. **端口错误**：RPC 端口（默认 20200）和 Channel 端口（默认 20200）不同，检查使用了正确的连接方式
3. **SSL 类型不对**：如果链开启了国密（SM SSL），SDK 也需要配置为国密模式

### Q10：Python SDK 安装失败"command 'gcc' failed"？

**A**：Python SDK 依赖 `cryptography` 库，需要编译环境：

```bash
# Ubuntu
sudo apt install python3-dev gcc libssl-dev

# CentOS
sudo yum install python3-devel gcc openssl-devel
```

## 四、网络和共识相关

### Q11：节点之间网络不通？

**A**：按以下顺序排查：
1. 检查 P2P 端口（默认 30300）是否在防火墙中开放
2. 使用 `telnet <其他节点IP> 30300` 测试连通性
3. 查看节点日志 `tail -f nodes/127.0.0.1/node0/log/log_*.log | grep -i "connect\|p2p"`
4. 确保创世块配置（config.genesis）在各节点上完全一致

### Q12：查看日志发现大量"ViewChangeWarning"？

**A**：这是 PBFT 共识的视图切换警告，表示共识节点之间通信出现延迟或异常：

1. 检查网络延迟和带宽
2. 检查 CPU 使用率是否过高（共识需要即时计算）
3. 查看是否有机器的系统时间与其他节点不同步（使用 NTP）
4. 偶发的视图切换是正常的，频繁出现则需要排查

### Q13：区块高度不动了，怎么办？

**A**：按以下顺序排查：
1. 检查是否有共识节点在正常运行：`tail -f log/*.log | grep "seal"`
2. 检查交易池是否有堆积：查看 `txpool` 相关日志
3. 检查是否有至少 2/3 的共识节点在线（PBFT 共识要求）
4. 使用 `getSyncStatus` 查看节点同步状态

## 五、性能优化相关

### Q14：TPS 上不去，怎么优化？

**A**：性能优化可以从以下方面入手：

1. **硬件层面**：使用 SSD、增加 CPU 核心数、增加内存
2. **网络层面**：确保共识节点之间网络延迟 < 10ms
3. **合约层面**：
   - 使用预编译合约代替 Solidity 合约（性能可提升 10-100 倍）
   - 启用 DMC（确定性多合约并行）并行执行
4. **配置层面**：
   - 调整交易池大小 `[txpool].limit`
   - 调整区块 Gas 上限
   - 关闭不必要的日志输出

### Q15：区块数据和日志占用大量磁盘空间怎么办？

**A**：
1. 使用 `archive-tool` 工具归档历史区块数据
2. 配置日志轮转策略，修改 `config.ini` 中的日志配置
3. 对于 Max 版本，使用 TiKV 集群版，利用其自动压缩功能
4. 定期清理 `nodes/*/log/` 下的旧日志文件

## 六、安全和权限相关

### Q16：私钥管理不当可能导致什么风险？

**A**：
- 私钥泄露 = 资产和权限失控，攻击者可以发起任意交易
- 私钥丢失 = 账户永久无法使用，资产无法找回
- **最佳实践**：
  - 生产环境使用 HSM（硬件安全模块）或 KMS（密钥管理服务）
  - 开发环境不要使用生产私钥
  - 定期轮换私钥（通过治理委员会）

### Q17：如何实现权限管控？

**A**：FISCO BCOS 支持多层次的权限控制：
1. **链级权限**：通过治理委员会控制共识节点加入/退出
2. **合约级权限**：通过合约内部的 `require(msg.sender == owner)` 模式
3. **表级权限**：通过 CRUD 预编译合约设置表的读写权限
4. **SDK 级权限**：通过 TLS 证书认证客户端身份

## 七、版本升级相关

### Q18：如何升级 FISCO BCOS 版本？

**A**：
1. **小版本升级**（如 3.6.0 → 3.7.0）：替换二进制文件，重启节点即可
2. **大版本升级**（如 2.x → 3.x）：需要数据迁移，无法平滑升级
3. **兼容性版本**：`compatibility_version` 在建链时确定，后续不可更改。升级节点时此配置必须保持不变

升级步骤：
```bash
# 1. 停止节点
bash nodes/127.0.0.1/stop_all.sh
# 2. 备份数据
cp -r nodes nodes_backup
# 3. 替换二进制
cp new_fisco-bcos nodes/127.0.0.1/node0/fisco-bcos
# 4. 重启节点
bash nodes/127.0.0.1/start_all.sh
```

### Q19：升级后节点无法启动？

**A**：
1. 检查 `compatibility_version` 是否匹配
2. 检查配置文件的格式是否有变更（对比新旧版本的配置模板）
3. 检查创世块配置是否未被修改
4. 查看启动日志中的具体错误信息

## 八、获取帮助

如果以上 FAQ 未能解决你的问题，可以通过以下渠道寻求帮助：

- **官方文档**：[https://fisco-bcos-documentation.readthedocs.io/](https://fisco-bcos-documentation.readthedocs.io/)
- **GitHub Issues**：[https://github.com/FISCO-BCOS/FISCO-BCOS/issues](https://github.com/FISCO-BCOS/FISCO-BCOS/issues)
- **技术交流群**：关注 FISCO BCOS 开源社区公众号获取最新群号
- **社区论坛**：[https://github.com/FISCO-BCOS/FISCO-BCOS/discussions](https://github.com/FISCO-BCOS/FISCO-BCOS/discussions)

---

*本文档由社区贡献者整理，持续更新中。欢迎通过 PR 补充更多问题和答案。*
