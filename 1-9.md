# AMOP 发布消息样例源码走读

## 前言

AMOP（Advanced Messages On-chain Protocol）是 FISCO BCOS 提供的链外消息通信协议。它允许链上的机构节点之间通过安全加密的 P2P 通道直接传递消息，而不需要将消息上链。AMOP 提供了三种消息模式：

- **publish**（点对点发布）：向订阅了指定 topic 的节点发送消息，并接收响应
- **subscribe**（订阅）：订阅指定 topic，接收发布者发来的消息
- **broadcast**（广播）：向全网所有订阅了指定 topic 的节点广播消息，不关心响应

`publish.cpp` 演示了 AMOP 的 publish 模式，本文将从源码角度讲解其实现原理。

## 整体流程概览

AMOP publish 的执行流程：

1. 解析命令行参数（配置文件 + topic + message）
2. 初始化 SDK 并建立连接
3. 进入循环，每 5 秒发送一次消息
4. 通过 `amop()->publish()` 发送消息并注册回调
5. 在回调中处理响应或错误

## 分步骤源码解析

### 第一步：usage 函数（第 40-47 行）

```cpp
void usage()
{
    std::cerr << "Desc: publish amop message by command params\n";
    std::cerr << "Usage: publish <config> <topic> <message>\n"
              << "Example:\n"
              << "    ./publish ./config_sample.ini topic HelloWorld\n";
    std::exit(0);
}
```

publish 模式需要三个参数：

| 参数 | 类型 | 说明 |
|------|------|------|
| config | string | SDK 配置文件路径 |
| topic | string | AMOP 话题名，订阅者和发布者通过 topic 建立关联 |
| message | string | 要发送的消息内容 |

### 第二步：命令行参数解析（第 51-61 行）

```cpp
    std::string config = argv[1];
    std::string topic = argv[2];
    std::string msg = argv[3];

    std::cout << LOG_DESC(" [AMOP][Publish]] params ===>>>> ")
              << LOG_KV("\n\t # config", config)
              << LOG_KV("\n\t # topic", topic)
              << LOG_KV("\n\t # message", msg) << std::endl;
```

三个参数直接从 `argv` 获取并打印确认。注意这里有一个字符级的小问题——日志标签 `[AMOP][Publish]]` 多了一个右方括号，在调试输出中可能会引起注意。

### 第三步：SDK 初始化（第 63-69 行）

```cpp
    auto factory = std::make_shared<SdkFactory>();
    // construct cpp-sdk object
    auto sdk = factory->buildSdk(config);
    // start sdk
    sdk->start();

    std::cout << LOG_DESC(" [AMOP][Publish] start sdk ... ") << std::endl;
```

标准的 SDK 三段式初始化：工厂 → 构建 → 启动。AMOP 功能依赖于 SDK 的 WebSocket 连接，因此需要先启动 SDK。

### 第四步：AMOP Publish 核心调用（第 71-106 行）

```cpp
    int i = 0;
    while (true)
    {
        std::cout << LOG_DESC(" publish message ===>>>> ")
                  << LOG_KV("topic", topic)
                  << LOG_KV("message", msg) << std::endl;

        sdk->amop()->publish(topic,
            bytesConstRef((byte*)msg.data(), msg.size()),
            -1,
            [](Error::Ptr _error,
                std::shared_ptr<bcos::boostssl::ws::WsMessage> _msg,
                std::shared_ptr<bcos::boostssl::ws::WsSession> _session) {
                // 回调处理...
            });
        std::this_thread::sleep_for(std::chrono::milliseconds(5000));
        i++;
    }
```

这是消息发送的主循环，每 5 秒发送一次。核心是 `sdk->amop()->publish()` 调用，让我们分解其每一个参数。

### 第五步：publish 参数详解（第 77-103 行）

```cpp
sdk->amop()->publish(
    topic,                                              // 参数1: 话题名
    bytesConstRef((byte*)msg.data(), msg.size()),       // 参数2: 消息数据
    -1,                                                 // 参数3: 超时时间(ms)
    [](Error::Ptr _error,
       std::shared_ptr<bcos::boostssl::ws::WsMessage> _msg,
       std::shared_ptr<bcos::boostssl::ws::WsSession> _session) { ... }  // 参数4: 回调
);
```

**参数 1 — topic（话题名）**：

topic 是 AMOP 通信的关键标识。只有订阅了相同 topic 的节点才能收到消息。Topic 可以是任意字符串，在联盟链场景中一般按业务场景命名，如 `"trade_notification"`、`"audit_log"` 等。

**参数 2 — bytesConstRef（消息数据）**：

```cpp
bytesConstRef((byte*)msg.data(), msg.size())
```

将 `std::string` 类型的消息转换为 `bytesConstRef`（FISCO BCOS 中的字节序列引用）。`bytesConstRef` 是一个轻量级的只读视图，不拷贝数据，只持有指针和长度。消息内容可以是文本、JSON、Protobuf 序列化数据等任意二进制内容。

**参数 3 — -1（超时时间）**：

`-1` 表示使用默认超时时间（通常由 SDK 配置中的 `message_timeout_ms` 决定）。如果改为正数（如 5000），则表示等待响应的最长时间为 5000 毫秒，超时后回调仍会被触发，但 `_error` 将包含超时错误。

**参数 4 — 回调函数**：

这是 publish 模式与 broadcast 模式最核心的区别——publish **期待并处理响应**。回调函数接收三个参数：

| 参数 | 类型 | 含义 |
|------|------|------|
| `_error` | `Error::Ptr` | 错误对象，发送失败时非空 |
| `_msg` | `WsMessage` | 响应消息，包含 status 和 payload |
| `_session` | `WsSession` | WebSocket 会话对象（此示例中忽略） |

### 第六步：回调中的双重错误检查（第 81-102 行）

```cpp
[](Error::Ptr _error, std::shared_ptr<bcos::boostssl::ws::WsMessage> _msg,
   std::shared_ptr<bcos::boostssl::ws::WsSession> _session) {
    boost::ignore_unused(_session);

    if (_error)
    {
        // 第一层检查：通信层错误
        std::cout << " \t something is wrong"
                  << LOG_KV("error", _error->errorCode())
                  << LOG_KV("errorMessage", _error->errorMessage()) << std::endl;
        return;
    }
    else
    {
        if (_msg->status() != 0)
        {
            // 第二层检查：应用层错误
            std::cout << " \t something is wrong"
                      << LOG_KV("error", _msg->status())
                      << LOG_KV("errorMessage",
                           std::string(_msg->payload()->begin(), _msg->payload()->end()))
                      << std::endl;
            return;
        }

        // 成功：打印响应内容
        std::cout << " \t recv response message ===>>>> "
                  << LOG_KV("msg",
                       std::string(_msg->payload()->begin(), _msg->payload()->end()))
                  << std::endl;
    }
}
```

这个回调设计了两层错误检查：

1. **通信层错误（`_error`）**：表示网络层面的错误，如 WebSocket 连接断开、消息发送失败等。`_error->errorCode()` 返回具体的错误码。

2. **应用层错误（`_msg->status()`）**：即使消息成功送达，订阅方处理消息时也可能返回非零的 status，表示应用层面的拒绝或处理失败。`status != 0` 时，`_msg->payload()` 包含订阅方返回的错误描述。

这种双层检查设计确保了发布者能准确区分"消息没发出去"和"消息发了但对方处理失败"两种场景。

### 第七步：循环间隔（第 104 行）

```cpp
std::this_thread::sleep_for(std::chrono::milliseconds(5000));
```

每 5 秒发送一次消息。实际生产中可根据业务需求调整频率。

## 总结

`publish.cpp` 展示了 AMOP publish 模式的核心用法：

1. **三个关键参数**：topic（话题）、message（消息体）、callback（响应处理）
2. **bytesConstRef**：轻量级的二进制数据视图，支持任意格式的消息内容
3. **双层错误检查**：通信层 `_error` + 应用层 `_msg->status()`，提供精细化的错误诊断
4. **响应机制**：publish 的核心优势——能获取每个订阅者的响应，适合需要确认和回执的场景

AMOP 的 publish 模式适用于需要**点对点可靠通信**的场景，如机构间的业务通知、状态同步等。如果需要一对多广播且不关心响应，则应使用 broadcast 模式。
