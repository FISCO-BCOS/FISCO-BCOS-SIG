# FISCO BCOS 样例代码总览

## 一、前言

FISCO BCOS 作为一个成熟的国产开源联盟链平台，为开发者提供了丰富的样例代码。这些样例分布在多个仓库和目录中，涵盖了从合约部署、RPC 调用、事件订阅到 P2P 网络编程的完整技术栈。

对于刚接触 FISCO BCOS 的新同学来说，面对众多的样例代码，常常不知从何入手。本文按照**技术层次**和**学习曲线**将这些样例进行分类梳理，帮助每位开发者快速找到适合自己的学习入口。

## 二、SDK 层样例（bcos-cpp-sdk/sample）

SDK 层是最常用的开发入口。这些样例展示了如何使用 C++ SDK 与 FISCO BCOS 节点进行交互。按照业务场景，可分为四大类：

### 2.1 合约部署与性能测试（sample/tx/）

| 样例文件 | 功能描述 | 难度 |
|---------|---------|------|
| `deploy_hello.cpp` | HelloWorld 合约的完整部署流程：SDK 初始化、GroupInfo 校验、KeyPair 生成、sendTransaction、回执解析 | ⭐⭐ |
| `hello_perf.cpp` | HelloWorld 合约调用性能测试：循环调用 set/get 方法，统计 TPS | ⭐⭐⭐ |
| `random_perf.cpp` | 随机交易性能测试：模拟随机交易对性能的影响 | ⭐⭐⭐ |
| `tx_sign_perf.cpp` | 交易签名性能基准测试：衡量密码学运算的吞吐量 | ⭐⭐⭐ |

**适合人群**：
- `deploy_hello.cpp` 是**入门必读**，涵盖了 SDK 的所有核心概念
- 性能测试系列适合需要做**性能调优和容量规划**的开发者

### 2.2 RPC 调用（sample/rpc/）

| 样例文件 | 功能描述 | 难度 |
|---------|---------|------|
| `blocknotifier.cpp` | 注册区块高度通知回调，实时获取最新区块号。演示了 registerBlockNumberNotifier 的注册和回调机制 | ⭐⭐ |
| `rpc_test.cpp` | 完整的 RPC 调用示例：命令行参数解析、WsConfig 初始化、getBlockNumber 调用、多线程并发测试 | ⭐⭐⭐ |

**适合人群**：
- `blocknotifier.cpp` 适合需要**实时监控链状态**的开发者（如区块浏览器后端）
- `rpc_test.cpp` 适合需要深入理解 **SDK 初始化链路和连接管理**的开发者

### 2.3 事件订阅（sample/eventsub/）

| 样例文件 | 功能描述 | 难度 |
|---------|---------|------|
| `eventsub.cpp` | 事件订阅完整示例：from/to 区块范围参数、address 过滤、EventSubParams 构建、subscribeEvent 回调处理 | ⭐⭐⭐ |

**适合人群**：
- 需要**监听链上合约事件**的 DApp 后端开发者
- 需要**同步链上数据到链下数据库**的数据服务开发者

### 2.4 AMOP 消息通信（sample/amop/）

| 样例文件 | 功能描述 | 难度 |
|---------|---------|------|
| `publish.cpp` | AMOP 点对点消息发布：topic + message 发送 + 双层错误检查 + 响应回调处理 | ⭐⭐ |
| `subscribe.cpp` | AMOP 消息订阅：多 topic 订阅 + setSubCallback 注册 + sendResponse 响应回传（Echo） | ⭐⭐ |
| `broadcast.cpp` | AMOP 消息广播：fire-and-forget 模式，与 publish 模式对比分析 | ⭐⭐ |

**适合人群**：
- 需要**机构间链下通信**的开发者（如交易通知、清算指令）
- `broadcast.cpp` 与 `publish.cpp` 的对比能帮助理解**通信模式选型**

## 三、网络层样例（fisco-bcos-demo）

网络层样例越过 SDK 和共识层，直接在 P2P 网络层进行编程。

| 样例文件 | 功能描述 | 难度 |
|---------|---------|------|
| `echo_server_sample.cpp` | Echo Server：Gateway 构建、P2P 消息处理器注册（registerHandlerByMsgType）、setRespPacket + asyncSendMessage 响应 | ⭐⭐⭐ |
| `echo_client_sample.cpp` | Echo Client：与 Echo Server 配对使用，发送自定义类型的 P2P 消息并等待响应 | ⭐⭐⭐ |
| `distributed_ratelimiter_checker.cpp` | 分布式限流检查器：展示如何在 P2P 网络中实现分布式协调 | ⭐⭐⭐⭐ |

**适合人群**：
- 需要**自定义 P2P 协议**的底层开发者
- 需要构建**链下分布式服务**（如分布式限流、分布式调度）的架构师

**场景举例**：
- Echo Server/Client 是理解 P2P 消息流的经典教程
- 分布式限流器展示了如何在 P2P 网络基础上构建实用的中间件

## 四、工具集（tools/）

FISCO BCOS 的工具集提供了链的构建、运维和管理能力。

| 工具目录 | 功能描述 |
|---------|---------|
| `BcosAirBuilder/` | Air 版链构建工具：一键部署轻量级联盟链，适合开发和测试环境 |
| `BcosBuilder/` | Pro/Max 版链构建工具：支持多机部署、Tars 服务治理、TiKV 分布式存储 |
| `storage-tool/` | 存储管理工具：数据导入导出、快照管理、存储迁移 |
| `archive-tool/` | 归档工具：历史区块数据归档，释放存储空间 |
| `hsm-tool/` | 硬件安全模块（HSM）工具：集成密码机进行密钥管理 |
| `kms-tool/` | 密钥管理服务（KMS）工具：云端密钥管理 |
| `log_extract.sh` | 日志提取脚本：从节点日志中提取关键信息 |
| `summary.sh` | 节点状态摘要脚本：快速查看节点运行状态 |

## 五、学习路线推荐

### 入门路径（1-2 周）

```
deploy_hello.cpp → blocknotifier.cpp → eventsub.cpp
```

1. 从 `deploy_hello.cpp` 开始：理解 SDK 初始化、合约部署、交易发送的完整流程
2. 接着看 `blocknotifier.cpp`：学习事件驱动的编程模型，理解回调机制
3. 最后看 `eventsub.cpp`：深入事件系统，掌握订阅管理和参数控制

### 进阶路径（2-4 周）

```
AMOP 三件套 → rpc_test.cpp → echo_server_sample.cpp
```

1. 按 publish → subscribe → broadcast 顺序学习 AMOP：理解机构间通信的三种模式
2. `rpc_test.cpp` 带你深入 SDK 底层：连接管理、SSL 配置、多线程模型
3. `echo_server_sample.cpp` 让你接触到 P2P 网络层：消息类型注册、Gateway 架构

### 运维路径

```
tools/BcosAirBuilder → tools/storage-tool → 性能测试系列
```

1. 使用 BcosAirBuilder 部署测试链
2. 通过性能测试系列（hello_perf、random_perf、tx_sign_perf）评估链的性能
3. 使用 storage-tool 和 archive-tool 进行长期运维

## 六、总结

FISCO BCOS 的样例代码体系覆盖了从应用层到网络层的全栈场景：

| 层次 | 样例范围 | 学习价值 |
|------|---------|---------|
| **应用层** | deploy_hello, eventsub | 最贴近业务开发，入门首选 |
| **通信层** | AMOP 三件套 | 机构间链下通信 |
| **SDK 底层** | rpc_test, blocknotifier | 深入连接管理和配置 |
| **网络层** | echo_server/client | P2P 自定义协议开发 |
| **运维层** | tools/ | 链的部署与运维 |

建议新同学按照「入门路径 → 选择自己感兴趣的方向深入」的方式进行学习。每个样例都附带了详细的源码走读教程（见本系列的其他文章），配合源码阅读效果更佳。
