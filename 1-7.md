# 一次 RPC 调用是如何发起的 —— FISCO BCOS C++ SDK 源码剖析

## 前言

在 FISCO BCOS 区块链系统中，**RPC（Remote Procedure Call，远程过程调用）** 是客户端与区块链节点之间最核心的通信桥梁。无论是查询区块高度、部署合约、发送交易，还是获取共识状态，所有对链上数据的读写操作都通过 RPC 调用来完成。FISCO BCOS 的 C++ SDK（`bcos-cpp-sdk`）封装了一套完整的 RPC 通信框架，底层基于 WebSocket 长连接协议，并支持 SSL/SM_SSL 加密通道，为开发者提供了类型安全的异步调用接口。

`rpc_test.cpp` 是 SDK 中一个精简但极其典型的示例程序，它演示了从命令行参数解析、SDK 配置初始化、WebSocket 连接建立、到最终发起 `getBlockNumber` RPC 请求的完整生命周期。本文将以该文件为核心，结合相关联的 `SdkFactory`、`JsonRpcImpl`、`WsConfig` 等源码，逐层剖析一次 RPC 调用从发起到响应的全过程。

---

## 命令行参数解析

程序的入口从 `main()` 函数（159 行）开始，第一件事便是解析命令行参数：

```cpp
if (argc < 5)
{
    usage();
}
const char* host = argv[1];
int port = atoi(argv[2]);
const char* type = argv[3];
const char* group = argv[4];
```

程序要求用户传入 4 个参数，不足时调用 `usage()` 打印帮助信息并退出：

```
Usage: rpc <host> <port> <ssl type> <group_id>
Example:
   ./rpc 127.0.0.1 20200 ssl group0
   ./rpc 127.0.0.1 20200 sm_ssl group0
```

四个参数的含义如下：

- **host**：目标 FISCO BCOS 节点的 IP 地址，支持 IPv4、IPv6 或域名。
- **port**：节点的 WebSocket 监听端口，通常为 `20200`。
- **ssl type**：加密通道类型，支持 `ssl`（标准 SSL）和 `sm_ssl`（国密 SSL）两种模式。
- **group_id**：多群组架构下的群组标识，如 `group0`、`group1`。

接着，程序通过 `strstr(type, "sm_ssl")` 判断是否启用了国密 SSL：

```cpp
int is_sm_ssl = 1;
char const* pos = strstr(type, "sm_ssl");
if (pos == NULL)
{
    is_sm_ssl = 0;
}
```

FISCO BCOS 同时支持标准 SSL/TLS 和国密 SM2/SM3/SM4 算法套件，这一步决定了后续 SSL 上下文（ContextConfig）将采用哪一套密码学原语。

---

## C 结构体定义：配置的中间表示

在深入 SDK 初始化逻辑之前，有必要理解程序定义的三层 C 风格结构体（42-73 行），它们构成了从原始参数到 SDK 内部配置对象的过渡桥梁。

### 第一层：`bcos_sdk_c_endpoint`（节点端点）

```cpp
struct bcos_sdk_c_endpoint
{
    std::string host;  // IPv4 / IPv6 / 域名
    uint16_t port;
};
```

这是最基础的原子结构，描述一个节点的网络坐标。host 和 port 的组合唯一标识了一条 WebSocket 连接目标。

### 第二层：`bcos_sdk_c_cert_config`（证书配置）

```cpp
struct bcos_sdk_c_cert_config
{
    std::string ca_cert;   // CA 根证书
    std::string node_key;  // SDK 私钥
    std::string node_cert; // SDK 证书
};
```

SSL 双向认证需要三份 PEM 格式的证书材料：CA 根证书用于验证服务端身份，node_cert 和 node_key 用于客户端身份认证。在 SM_SSL 模式下，这些证书对应的则是国密算法生成的证书。

### 第三层：`bcos_sdk_c_config`（完整配置）

```cpp
struct bcos_sdk_c_config
{
    int thread_pool_size;
    int message_timeout_ms;
    int reconnect_period_ms;
    int heartbeat_period_ms;
    int send_rpc_request_to_highest_block_node;
    std::vector<bcos_sdk_c_endpoint> peers;
    size_t peers_count;
    int disable_ssl;
    std::string ssl_type;
    int is_cert_path;
    bcos_sdk_c_cert_config cert_config;
};
```

这是顶层配置的聚合体，包含了 SD K 运行所需的全部参数：

| 字段 | 含义 | 典型值 |
|------|------|--------|
| `thread_pool_size` | Boost.Asio 线程池大小 | 8 |
| `message_timeout_ms` | 消息发送超时（毫秒） | 10000 |
| `reconnect_period_ms` | 断线重连间隔（毫秒） | 默认不低于 10000 |
| `heartbeat_period_ms` | 心跳间隔（毫秒） | 默认不低于 10000 |
| `send_rpc_request_to_highest_block_node` | 是否将 RPC 请求发送到最高区块节点 | 1（开启） |
| `peers` | 连接的节点列表 | — |
| `disable_ssl` | 是否禁用 SSL | 示例中设置为 1 |
| `ssl_type` | SSL 类型字符串（`"ssl"` 或 `"sm_ssl"`） | — |
| `is_cert_path` | 证书字段是路径还是内容 | 1（路径） |

---

## `bcos_sdk_create_config()`：配置对象创建

函数 `bcos_sdk_create_config()`（125-158 行）将命令行参数组装为完整的 `bcos_sdk_c_config` 对象：

```cpp
auto config = std::make_shared<bcos_sdk_c_config>();
// 先全部置零，再逐项覆写 —— 防止未初始化的内存干扰
config->thread_pool_size = 0;
config->message_timeout_ms = 0;
config->heartbeat_period_ms = 0;
config->reconnect_period_ms = 0;
config->disable_ssl = 0;
config->send_rpc_request_to_highest_block_node = 1;
```

值得注意的设计细节：函数先将所有字段统一归零，再进行定向赋值。这是 C/C++ 混合编程中的一种防御性策略——确保即使结构体未来扩展了新字段，已分配的内存也不会残留随机值。

随后设置核心参数：

```cpp
config->thread_pool_size = 8;           // 8 个工作线程
config->message_timeout_ms = 10000;     // 10 秒超时
struct bcos_sdk_c_endpoint ep = {host, port};
config->peers.push_back(ep);
config->peers_count = 1;               // 连接单个节点
config->disable_ssl = 1;               // 示例中关闭 SSL（简化测试）
config->send_rpc_request_to_highest_block_node = 1;
config->ssl_type = sm_ssl ? "sm_ssl" : "ssl";
```

证书路径采用了 C99 的指定初始化器语法（designated initializer）：

```cpp
config->cert_config = {
    .ca_cert = "./conf/ca.crt",
    .node_key = "./conf/sdk.key",
    .node_cert = "./conf/sdk.crt"
};
```

这种写法按字段名赋值，比按位置初始化更清晰、更不易出错。

---

## `initWsConfig()`：C 结构体到 WsConfig 的桥梁

`initWsConfig()`（74-108 行）是整个程序架构中承上启下的关键函数。它接收 `bcos_sdk_c_config` 指针，输出 SDK 框架可直接使用的 `WsConfig` 对象。这一步完成了**从 C 风格数据到 C++ 面向对象配置**的语义转换。

### 步骤一：创建 WsConfig 并设置为客户端模式

```cpp
auto wsConfig = std::make_shared<bcos::boostssl::ws::WsConfig>();
wsConfig->setModel(bcos::boostssl::ws::WsModel::Client);
```

`WsModel` 是一个枚举量，定义在 `WsConfig.h` 中：

```cpp
enum WsModel : uint16_t
{
    None = 0,
    Client = 0x01,
    Server = 0x10,
    Mixed = Client | Server
};
```

SDK 作为请求的发起方，自然运行在 `Client` 模式下。`WsConfig` 还内置了最小值保护逻辑——例如 `reconnectPeriod()` 和 `heartbeatPeriod()` 的返回值均不低于 `MIN_RECONNECT_PERIOD_MS` 和 `MIN_HEART_BEAT_PERIOD_MS`（均为 10000 毫秒），防止过短的重连/心跳间隔导致资源耗尽。

### 步骤二：填充连接节点信息

```cpp
auto peers = std::make_shared<bcos::boostssl::ws::EndPoints>();
for (size_t i = 0; i < config->peers_count; i++)
{
    auto host = config->peers[i].host;
    auto port = config->peers[i].port;
    bcos::boostssl::NodeIPEndpoint endpoint = bcos::boostssl::NodeIPEndpoint(host, port);
    peers->insert(endpoint);
}
wsConfig->setConnectPeers(peers);
```

`EndPoints` 本质上是 `std::set<NodeIPEndpoint>` 的别名，使用 `set` 容器自动保证了节点列表的去重。`NodeIPEndpoint` 封装了 Boost.Asio 的 `ip::tcp::endpoint`，在底层负责将域名解析为 IP 地址。

### 步骤三：基础设施参数注入

```cpp
wsConfig->setDisableSsl(config->disable_ssl);
wsConfig->setThreadPoolSize(config->thread_pool_size);
wsConfig->setReconnectPeriod(config->reconnect_period_ms);
wsConfig->setHeartbeatPeriod(config->heartbeat_period_ms);
wsConfig->setSendMsgTimeout(config->message_timeout_ms);
```

这五行赋值将线程池规模、超时、重连、心跳四个维度的运行时参数注入 `WsConfig`。注意 `setThreadPoolSize(0)` 时，`threadPoolSize()` 的 getter 会返回最小值 1（`MIN_THREAD_POOL_SIZE`）——这是 WsConfig 的内建兜底策略。

### 步骤四：SSL 证书上下文配置（条件分支）

```cpp
if (!config->disable_ssl)
{
    auto contextConfig = std::make_shared<bcos::boostssl::context::ContextConfig>();
    contextConfig->setSslType(config->ssl_type);
    contextConfig->setIsCertPath(config->is_cert_path ? true : false);
    bcos::boostssl::context::ContextConfig::CertConfig certConfig;
    certConfig.caCert = config->cert_config.ca_cert;
    certConfig.nodeCert = config->cert_config.node_cert;
    certConfig.nodeKey = config->cert_config.node_key;
    contextConfig->setCertConfig(certConfig);
    wsConfig->setContextConfig(contextConfig);
}
```

只有当 `disable_ssl` 为假时，才会构建 SSL 上下文配置。`ContextConfig` 是 `bcos-boostssl` 模块的核心类，它封装了底层 SSL 库（如 OpenSSL 或 Tongsuo）的初始化参数。`setIsCertPath(true)` 告诉框架：传入的 `ca_cert`、`node_cert`、`node_key` 是**文件路径**而非证书 PEM 文本本身，框架将在内部完成文件读取。

---

## `thread_function()`：每线程独立的 SDK 生命周期

`thread_function()`（109-124 行）是并发测试的核心——每个线程内部都会走一遍完整的 SDK 生命周期：

```cpp
void* thread_function(std::shared_ptr<bcos_sdk_c_config> arg)
{
    while (1)
    {
        auto factory = std::make_shared<bcos::cppsdk::SdkFactory>();
        auto wsConfig = initWsConfig(arg);
        auto sdk = factory->buildSdk(wsConfig, arg->send_rpc_request_to_highest_block_node);
        sdk->start();
        auto rpc = sdk->jsonRpc();
        rpc->getBlockNumber(
            "group0", "", [](bcos::Error::Ptr error, std::shared_ptr<bcos::bytes> resp) {});
        usleep(100);
        sdk->stop();
        sdk.reset(nullptr);
    }
}
```

这个无限循环包含了六个阶段，我们逐一拆解：

### 阶段一：创建 SdkFactory

`SdkFactory` 的构造函数中只做了一件事——调用 `LogInitializer::initLog()` 初始化日志系统。它是一个纯粹的工厂类，不持有任何状态。

### 阶段二：构建 WsConfig

调用上文分析的 `initWsConfig()`，将 C 结构体配置转为 `WsConfig` 对象。

### 阶段三：`buildSdk()` 组装 SDK

`SdkFactory::buildSdk()`（`SdkFactory.cpp` 63-75 行）是整个组装流程的编排者：

```cpp
auto service = buildService(std::move(_config));       // 1. WebSocket 服务层
auto amop = buildAMOP(service);                         // 2. AMOP（链上链下消息通道）
auto jsonRpc = buildJsonRpc(service, _sendRequestToHighestBlockNode); // 3. JSON-RPC 核心
auto eventSub = buildEventSub(service);                  // 4. 事件订阅
auto jsonRpcService = buildJsonRpcService(jsonRpc);      // 5. 高级 RPC 封装
auto sdk = std::make_unique<bcos::cppsdk::Sdk>(service, jsonRpc, amop, eventSub, jsonRpcService);
```

- **buildService()**：创建 `Service` 对象，通过 `WsInitializer` 将 WsConfig 注入，建立 WebSocket 连接管理引擎。同时注册了 `BLOCK_NOTIFY` 和 `GROUP_NOTIFY` 两种消息处理器，用于接收区块链节点推送的区块通知和群组变更通知。
- **buildJsonRpc()**（117-146 行）：创建一个 `JsonRpcImpl` 对象，并为其设置 sender 回调函数：

```cpp
jsonRpc->setSender([_service](const std::string& _group, const std::string& _node,
                       const std::string& _request, auto&& _respFunc) {
    auto data = std::make_shared<bytes>(_request.begin(), _request.end());
    auto msg = _service->messageFactory()->buildMessage();
    msg->setSeq(_service->messageFactory()->newSeq());
    msg->setPacketType(bcos::protocol::MessageType::RPC_REQUEST);
    msg->setPayload(data);
    _service->asyncSendMessageByGroupAndNode(_group, _node, msg, Options(),
        [_respFunc](Error::Ptr _error, MessageFace::Ptr _msg, auto&& _session) {
            _respFunc(_error, _msg ? _msg->payload() : nullptr);
        });
});
```

此处的 `sender` 闭包是**整个 RPC 调用的数据面出口**：它将 JSON 请求字符串编码为 `bytes`，封装为 `RPC_REQUEST` 类型的消息，然后通过 `Service` 的 `asyncSendMessageByGroupAndNode` 方法异步发送到目标节点的指定群组。

### 阶段四：`sdk->start()`

`Sdk::start()` 依次启动四个子系统：

```cpp
m_service->start();      // 启动 WebSocket 连接（建立 TCP + SSL 握手）
m_jsonRpc->start();      // 启动 JSON-RPC 处理队列
m_amop->start();         // 启动 AMOP 消息通道
m_eventSub->start();     // 启动事件订阅监听
```

其中 `m_service->start()` 是关键的触发点：它驱动 `WsConnector` 与目标节点建立 WebSocket 连接。如果 SSL 已启用，还会在此阶段完成 TLS 握手和双向证书验证。

### 阶段五：发起 `getBlockNumber` 调用

```cpp
auto rpc = sdk->jsonRpc();
rpc->getBlockNumber(
    "group0", "",
    [](bcos::Error::Ptr error, std::shared_ptr<bcos::bytes> resp) {});
```

`jsonRpc()` 返回的是 `JsonRpcImpl` 的共享指针。`getBlockNumber` 是一个**异步回调式**方法，它的完整签名定义在 `JsonRpcImpl.h`（88-89 行）：

```cpp
void getBlockNumber(
    const std::string& _groupID, const std::string& _nodeName, RespFunc _respFunc) override;
```

参数含义：
- `_groupID`：目标群组，此处为 `"group0"`。
- `_nodeName`：目标节点名，空字符串表示由框架自动选择通配节点。
- `_respFunc`：响应回调，类型为 `std::function<void(Error::Ptr, std::shared_ptr<bytes>)>`。

调用链路如下：

1. `JsonRpcImpl::getBlockNumber()` 使用 `JsonRpcRequestFactory` 构造一个符合 JSON-RPC 2.0 规范的请求体（`{"jsonrpc":"2.0","method":"getBlockNumber","params":["group0"],"id":1}`）。
2. 调用 `m_sender` 闭包（即 `buildJsonRpc` 中设置的回调），将请求字符串转为 `RPC_REQUEST` 消息并异步发送。
3. Service 层通过 WebSocket 连接将二进制消息推送到节点。
4. 节点处理请求后，沿 WebSocket 通道返回响应。
5. Service 层收到响应后，触发 `RespFunc` 回调。

在这段示例代码中，回调函数体为空（不做任何处理），因为测试的核心目的是验证并发连接能力而非解析返回值。

### 阶段六：停止与清理

```cpp
usleep(100);
sdk->stop();
sdk.reset(nullptr);
```

`sdk->stop()` 按逆序关闭四个子系统，断掉 WebSocket 连接并释放线程资源。`sdk.reset(nullptr)` 显式析构 Sdk 对象——实际上这里存在一个循环引用隐患：如果不显式 reset，`JsonRpcImpl` 内部的 `m_sender` 闭包捕获的 `_service` shared_ptr 可能延迟释放，造成资源泄漏。源码中此处的显式 reset 体现了作者对生命周期管理的审慎思考。

---

## `main()`：多线程并发测试设计

`main()` 函数的最后一部分（184-201 行）是并发测试的编排逻辑：

```cpp
std::thread threads[100];
long t;
for (t = 0; t < 100; t++)
{
    printf("In main: creating thread %ld\n", t);
    threads[t] = std::thread(thread_function, config);
}
for (t = 0; t < 100; t++)
{
    threads[t].join();
}
```

设计要点：

1. **共享配置，独立 SDK**：100 个线程共享同一个 `config` 对象（通过 `shared_ptr` 传递），但每个线程内的 `thread_function` 都会创建自己独立的 `SdkFactory`、`WsConfig` 和 `Sdk` 实例。这意味着每个线程都维护一条独立的 WebSocket 连接。
2. **无限循环压测**：`thread_function` 内部的 `while(1)` 意味着每个线程都在不断地"创建 SDK → 建立连接 → 发送 RPC → 销毁 SDK"的循环中运行，用于检测 SDK 在极端场景下的稳定性（如频繁建连/断连场景下的资源泄漏、竞态条件等）。
3. **`std::thread` 而非 `pthread`**：源码中保留了注释掉的 `pthread_create` 代码，这是从 C 风格接口迁移到 C++ 标准库的痕迹。`std::thread` 提供了更安全的 RAII 语义和异常传播能力。
4. **`join()` 阻塞等待**：由于 `thread_function` 是无限循环，`join()` 实际上永远不会返回——这是一个持续运行的压测程序，需要外部信号（Ctrl+C）来终止。

---

## 完整调用链路总览

从用户输入命令到 RPC 响应返回，一条 `getBlockNumber` 调用经历了以下层次：

```
命令行参数 (host, port, ssl_type, group_id)
    │
    ▼
bcos_sdk_create_config()           ← 组装 C 结构体配置
    │
    ▼
initWsConfig()                     ← C 结构体 → WsConfig 转换
    │                               （WsModel::Client、EndPoints、SSL ContextConfig）
    ▼
SdkFactory::buildSdk()
    ├── buildService()             ← WebSocket Service + 消息处理器注册
    ├── buildJsonRpc()             ← JsonRpcImpl + sender 闭包（序列化 + 异步发送）
    ├── buildAMOP()                ← AMOP 消息通道
    ├── buildEventSub()            ← 事件订阅
    └── buildJsonRpcService()      ← 高级 RPC 封装（TransactionBuilder）
    │
    ▼
Sdk::start()
    ├── Service::start()           ← TCP + SSL 握手 + WebSocket 连接建立
    ├── JsonRpcImpl::start()       ← RPC 请求队列启动
    ├── AMOP::start()
    └── EventSub::start()
    │
    ▼
JsonRpcImpl::getBlockNumber()
    ├── JsonRpcRequestFactory      ← 构造 JSON-RPC 2.0 请求体
    └── m_sender 闭包              ← 封装为 RPC_REQUEST MessageFace
        │
        ▼
Service::asyncSendMessageByGroupAndNode()
    │
    ▼
WebSocket 帧                           ← 二进制消息经加密通道发送
    │
    ▼
FISCO BCOS 节点                         ← 处理请求，返回 JSON-RPC 响应
    │
    ▼
RespFunc 回调                           ← (Error::Ptr, bytes) 通知调用方
```

---

## 总结

`rpc_test.cpp` 虽然仅有两百余行代码，却精准地展示了 FISCO BCOS C++ SDK 中一次 RPC 调用所涉及的完整技术栈：

- **配置抽象层**：通过三层 C 结构体 (`bcos_sdk_c_endpoint` → `bcos_sdk_c_cert_config` → `bcos_sdk_c_config`) 实现了从原始参数到 SDK 配置的结构化过渡，兼顾了 C 语言接口的简洁性和 C++ 内部的灵活性。
- **连接管理**：`WsConfig` 作为 WebSocket 配置的单一事实来源，统一管理线程池、超时、重连、心跳等基础参数，并内建了最小值保护以防止不当配置。
- **SSL 双轨制**：通过 `ContextConfig` 同时支持标准 SSL 和国密 SM_SSL，体现了 FISCO BCOS 对中国密码学合规要求的深度适配。
- **工厂模式组装**：`SdkFactory` 以声明式的方式将 WebSocket 服务、JSON-RPC 引擎、AMOP 消息通道、事件订阅等子系统组合为完整的 Sdk 对象，各模块之间的耦合通过 shared_ptr 和回调闭包管理。
- **异步非阻塞调用**：`getBlockNumber` 的回调式 API 设计避免了同步阻塞等待，契合高并发场景的性能需求。
- **并发压测框架**：100 线程共享配置、各自独立建立连接的压测模型，为 SDK 的稳定性验证提供了简洁有效的方案。

理解这条调用链路，不仅有助于使用 FISCO BCOS C++ SDK 进行日常开发，也为深入定制 SDK 行为（如自定义 RPC 方法、替换传输层协议）奠定了扎实的基础。

---

*本文基于 FISCO BCOS `bcos-sdk/sample/rpc/rpc_test.cpp` 及相关联的 `SdkFactory.cpp`、`JsonRpcImpl.h`、`WsConfig.h`、`Sdk.h` 等源码文件撰写，所有行号引用均对应源码文件中的实际行号。*
