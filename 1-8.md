# 如何完成 FISCO BCOS 的第一个 PR

## 一、前言

开源社区的魅力在于「共建」。FISCO BCOS 作为国内最活跃的开源联盟链项目之一，其代码和文档都是由社区贡献者共同维护的。提交 PR（Pull Request）不仅是回馈社区的方式，也是提升个人技术影响力的捷径——你的名字将出现在项目的贡献者列表中。

本文将以一个**真实场景**为例：修复 FISCO BCOS 文档中的一个错别字，手把手带你完成从 Fork 到合并的完整 PR 流程。

## 二、准备工作

### 2.1 注册 GitHub 账号

如果你还没有 GitHub 账号，前往 [https://github.com](https://github.com) 注册。建议使用常用邮箱，并设置一个有辨识度的用户名。

### 2.2 安装 Git

Windows 用户推荐安装 [Git for Windows](https://git-scm.com/download/win)，macOS 用户使用 `brew install git`，Linux 用户使用系统包管理器。

安装后配置用户信息：

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

### 2.3 Fork 目标仓库

以 FISCO BCOS 文档仓库为例：

1. 打开 [https://github.com/FISCO-BCOS/FISCO-BCOS-DOC](https://github.com/FISCO-BCOS/FISCO-BCOS-DOC)
2. 点击右上角的 **Fork** 按钮
3. 选择 Fork 到你的个人账号下

Fork 完成后，你的账号下会有一个 `你的用户名/FISCO-BCOS-DOC` 仓库。

### 2.4 Clone 到本地

```bash
git clone https://github.com/你的用户名/FISCO-BCOS-DOC.git
cd FISCO-BCOS-DOC
```

### 2.5 设置 Upstream

添加原始仓库为 upstream，用于后续同步：

```bash
git remote add upstream https://github.com/FISCO-BCOS/FISCO-BCOS-DOC.git
git remote -v
# 应该看到：
# origin    https://github.com/你的用户名/FISCO-BCOS-DOC.git (fetch)
# origin    https://github.com/你的用户名/FISCO-BCOS-DOC.git (push)
# upstream  https://github.com/FISCO-BCOS/FISCO-BCOS-DOC.git (fetch)
# upstream  https://github.com/FISCO-BCOS/FISCO-BCOS-DOC.git (push)
```

## 三、找到适合的第一个 Issue

### 3.1 浏览 Issues

访问 [FISCO-BCOS-DOC Issues](https://github.com/FISCO-BCOS/FISCO-BCOS-DOC/issues)，使用标签过滤：

- **good first issue**：专门为新贡献者准备的任务
- **documentation**：文档相关问题
- **bug**：纯 Bug 修复

### 3.2 认领 Issue

在 Issue 页面下评论表示认领：

> I'd like to work on this issue. Could you please assign it to me?

或者：

> 我愿意处理这个问题，可以分配给我吗？

如果不确定是否存在已知 Issue，也可以自己发现文档中的问题（如错别字、格式错误），直接提交修复 PR。

## 四、创建分支和修改

### 4.1 同步最新代码

在开始修改前，确保本地代码与上游同步：

```bash
git checkout release-3
git fetch upstream
git rebase upstream/release-3
```

### 4.2 创建功能分支

```bash
git checkout -b fix-doc-typos
```

分支命名建议：
- `fix-xxx`：修复类改动
- `docs-xxx`：文档类改动
- `feat-xxx`：新功能
- `refactor-xxx`：重构

### 4.3 进行修改

**场景示例**：修复文档中的标点符号错误。

假设在 `3.x/zh_CN/docs/key_concepts.md` 中发现：

```markdown
<!-- 修复前 -->
性能的优化有两个方向,向上扩展（Scale up）和平行扩展（Scale out）。

<!-- 修复后 -->
性能的优化有两个方向，向上扩展（Scale up）和水平扩展（Scale out）。
```

我们修改了三个问题：
1. 英文逗号 `,` → 中文逗号 `，`
2. 「平行扩展」→「水平扩展」（Scale out 的正确中文翻译）
3. 加上英文括号与中文之间的空格

### 4.4 提交 Commit

```bash
git add .
git commit -m "docs: fix punctuation errors and terminology in key_concepts.md

- Replace English comma with Chinese comma in Chinese text
- Fix '平行扩展' to '水平扩展' for Scale out
- Add spaces between Chinese and English characters"
```

**Commit Message 规范**：

FISCO BCOS 项目通常遵循约定式提交（Conventional Commits）：

```
<type>: <简短描述>

<详细说明（可选）>
```

常用 type：
- `docs`：文档修改
- `fix`：Bug 修复
- `feat`：新功能
- `refactor`：重构
- `test`：测试相关
- `chore`：构建/工具链修改

## 五、推送和创建 PR

### 5.1 Push 到你的 Fork

```bash
git push origin fix-doc-typos
```

如果 push 被拒绝（比如你的分支落后于 upstream），先 rebase：

```bash
git fetch upstream
git rebase upstream/release-3
git push origin fix-doc-typos -f
```

### 5.2 在 GitHub 上创建 PR

1. 打开你的 Fork 仓库页面
2. GitHub 通常会显示一个黄色的提示条「fix-doc-typos had recent pushes」，点击 **Compare & pull request**
3. 确认：
   - **base repository**：`FISCO-BCOS/FISCO-BCOS-DOC`
   - **base branch**：`release-3`（或对应版本分支）
   - **head repository**：`你的用户名/FISCO-BCOS-DOC`
   - **head branch**：`fix-doc-typos`

### 5.3 填写 PR 描述

一个好的 PR 描述应该包括：

```markdown
## 问题描述

修复了中文文档中的标点符号错误和术语翻译问题。

## 修改内容

- key_concepts.md: 修复英文逗号混用中文逗号的问题
- key_concepts.md: 修正"平行扩展"为"水平扩展"
- support_os.md: 修复标题中缺少空格的问题

## 修改类型

- [x] 文档修改

## 测试说明

- 纯文档修改，无需功能测试
- 已在本地预览确认格式正确
```

### 5.4 关联 Issue

如果有关联的 Issue，在描述中使用 `Closes #123` 或 `Fixes #123` 自动关联。

## 六、Code Review 流程

### 6.1 等待 Review

PR 提交后，项目的维护者会在 1-3 个工作日内进行 Review。在此期间 CI（持续集成）会自动运行检查：
- Markdown 格式检查
- 链接有效性检查
- 拼写检查（如配置了的话）

### 6.2 处理 Review Comments

如果维护者提出了修改建议：

1. 在本地分支上进行修改
2. 提交新的 commit：
   ```bash
   git add .
   git commit -m "docs: address review comments"
   git push origin fix-doc-typos
   ```
3. 新 commit 会自动追加到 PR 中
4. 在 GitHub 上回复 comment，点击 **Resolve conversation**

### 6.3 CLA（贡献者许可协议）

首次贡献 FISCO BCOS 时，需要签署 CLA。GitHub 机器人会在你的 PR 中自动检查：

- 如果未签署，点击 bot 评论中的链接完成电子签名
- CLA 只需签署一次，后续 PR 不需要重复签署

### 6.4 DCO 签名

FISCO BCOS 部分仓库需要 DCO（Developer Certificate of Origin）签名。如果 CI 提示 DCO 检查失败：

```bash
git commit --amend -s  # -s 添加 Signed-off-by 行
git push origin fix-doc-typos -f
```

## 七、PR 合并后

### 7.1 同步 Upstream

PR 被合并后，你的 fork 仓库会落后于上游：

```bash
git checkout release-3
git fetch upstream
git rebase upstream/release-3
git push origin release-3
```

### 7.2 删除功能分支

```bash
git branch -d fix-doc-typos           # 删除本地分支
git push origin --delete fix-doc-typos  # 删除远程分支
```

### 7.3 庆祝！

你的名字已经出现在 [FISCO BCOS 贡献者列表](https://github.com/FISCO-BCOS/FISCO-BCOS-DOC/graphs/contributors)中。

## 八、常见问题和技巧

### 8.1 CI 检查失败怎么办？

1. 点进 PR 页面底部的 **Checks** 标签
2. 查看失败的具体原因
3. 如果是格式问题，按提示修改后 push 新 commit
4. 如果 CI 失败原因不清楚，在 PR 中评论 `@ maintainer CI 失败可能是什么原因？`

### 8.2 Rebase vs Merge

- **推荐 Rebase**：保持 commit 历史线性、干净
  ```bash
  git fetch upstream
  git rebase upstream/release-3
  ```
- **避免在 PR 中 Merge**：会在提交历史中产生不必要的 merge commit

### 8.3 一个 PR 做一件事

每个 PR 应该聚焦一个明确的改动：
- ✅ 好的 PR：「修复 3 个文件中的标点符号错误」
- ❌ 不好的 PR：「修复标点 + 新增章节 + 修改样式 + 重构索引」

小步快跑更容易通过 Review。

### 8.4 如何回滚一个错误的 Commit

```bash
# 回滚最近一次 commit，保留修改内容
git reset --soft HEAD~1

# 完全回滚，丢弃修改
git reset --hard HEAD~1
```

## 九、总结与鼓励

完成第一个 PR 只需要五步：

1. **Fork** → 2. **Clone** → 3. **Branch & Fix** → 4. **Push & PR** → 5. **Review & Merge**

从修复一个错别字开始，逐步参与到代码修复、功能开发中去。FISCO BCOS 社区欢迎每一位贡献者，无论改动大小，每一次提交都在让这个国产开源平台变得更好。

**开始你的第一个 PR 吧！**
