# Echo Server 网络样例源码走读

## 前言

Echo Server 是计算机网络中最经典的"Hello World"程序——收到什么就返回什么。在 FISCO BCOS 中，`echo_server_sample.cpp` 展示了如何使用底层 P2P 网络框架构建一个自定义消息处理器。它跳过智能合约和共识层，直接在 P2P 网络层接收和响应消息，是理解 FISCO BCOS 网络层架构的绝佳入口。

## 整体流程概览

Echo Server 的启动和运行分为以下步骤：

1. **全局配置初始化** — 设置协议编解码器
2. **节点配置加载** — 从 config.ini 读取 P2P 网络配置
3. **日志初始化** — 配置 Boost 日志系统
4. **Gateway 构建** — 通过 GatewayFactory 创建网关实例
5. **P2P 消息处理器注册** — 注册自定义消息类型的回调
6. **Gateway 启动** — 开始监听 P2P 消息
7. **主线程保活** — 无限循环保持进程运行

## 分步骤源码解析

### 第一步：usage 函数（第 32-38 行）

```cpp
void usage()
{
    std::cerr << "Usage: echo-server-sample <listenIP> <listenPort> <ssl>\n"
              << "Example:\n"
              << "./echo-server-sample\n";
    std::exit(0);
}
```

使用说明接受三个参数：监听 IP、监听端口、是否启用 SSL。但实际 main 函数中并未使用命令行参数，而是从配置文件读取。

### 第二步：全局配置与密码学初始化（第 42-43 行）

```cpp
    g_BCOSConfig.setCodec(
        std::make_shared<bcostars::protocol::ProtocolInfoCodecImpl>());
    auto keyFactory = std::make_shared<bcos::crypto::KeyFactoryImpl>();
```

**g_BCOSConfig** 是 FISCO BCOS 的全局配置单例：

- `setCodec(ProtocolInfoCodecImpl)`：设置协议信息编解码器，用于序列化和反序列化 P2P 消息中的协议版本、群组信息等元数据。`ProtocolInfoCodecImpl` 是基于 Tars 协议的实现。

- `KeyFactoryImpl`：密钥工厂，提供密钥生成、签名验证等密码学能力。虽然此样例未直接使用，但在真实网关中签名验证是必需的。

### 第三步：节点配置加载（第 44-46 行）

```cpp
    auto nodeConfig = std::make_shared<NodeConfig>();
    std::string configFilePath = "config.ini";
    nodeConfig->loadConfig(configFilePath);
```

`NodeConfig` 封装了节点的所有配置：P2P 监听地址、端口、证书路径、群组列表等。配置文件格式为标准 INI，典型内容如下：

```ini
[p2p]
listen_ip=0.0.0.0
listen_port=30300
; SM SSL 配置
; sm_ca_cert=...
; sm_node_key=...

[cert]
ca_cert=./conf/ca.crt
node_key=./conf/ssl.key
node_cert=./conf/ssl.crt
```

### 第四步：日志初始化（第 48-51 行）

```cpp
    auto logInitializer = std::make_shared<BoostLogInitializer>();
    boost::property_tree::ptree pt;
    boost::property_tree::read_ini(configFilePath, pt);
    logInitializer->initLog(pt);
```

使用 Boost.Log 作为日志框架：

1. 读取 config.ini 为 `property_tree`（属性树）
2. 将属性树传给 `BoostLogInitializer::initLog()` 进行日志配置

通过 `property_tree` 的方式，日志配置可以与节点配置共享同一个 INI 文件。

### 第五步：Gateway 构建（第 53-57 行）

```cpp
    GatewayFactory gatewayFactory(nodeConfig->chainId(), "localClient", nullptr);
    auto gateway = gatewayFactory.buildGateway(
        configFilePath, true, nullptr, "localClient");
    auto service = std::dynamic_pointer_cast<Service>(gateway->p2pInterface());

    gateway->start();
```

这是整个 Echo Server 的核心部分，分为四步：

**Step 1 — 创建 GatewayFactory**：

```cpp
GatewayFactory gatewayFactory(chainId, "localClient", nullptr);
```

- `chainId`：链标识，从配置中读取，用于在网络中标识这条链
- `"localClient"`：网关名，用于日志和监控
- `nullptr`：第三个参数是 `DiskAccessoryFactory`，此示例中不使用磁盘存储

**Step 2 — 构建 Gateway**：

```cpp
auto gateway = gatewayFactory.buildGateway(configFilePath, true, nullptr, "localClient");
```

Gateway（网关）是 FISCO BCOS 网络层的核心组件，负责：
- P2P 连接的建立和维护
- 消息的收发和路由
- SSL/SM_SSL 安全连接的加密
- 协议版本的协商

第二个参数 `true` 表示启用 SSL。

**Step 3 — 获取 P2P Service 接口**：

```cpp
auto service = std::dynamic_pointer_cast<Service>(gateway->p2pInterface());
```

`p2pInterface()` 返回 P2P 模块的抽象接口，通过 `dynamic_pointer_cast` 转为具体的 `Service` 类型。`Service` 是 P2P 服务的具体实现，提供消息处理器注册等功能。

**Step 4 — 启动 Gateway**：

```cpp
gateway->start();
```

启动后 Gateway 开始监听 P2P 端口，接受来自其他节点的连接。

### 第六步：P2P 消息处理器注册（第 60-71 行）

```cpp
    service->registerHandlerByMsgType(
        999,
        [](NetworkException _e,
           std::shared_ptr<P2PSession> _session,
           P2PMessage::Ptr _msg) {
            if (_e.errorCode())
            {
                return;
            }
            auto startT = utcTime();
            _msg->setRespPacket();
            _session->session()->asyncSendMessage(_msg);
            BCOS_LOG(INFO)
                << LOG_DESC("sendResponse")
                << LOG_KV("timeCost", (utcTime() - startT))
                << LOG_KV("msgSize", (_msg->payload()->size()));
        });
```

这是 Echo Server 的心脏！`registerHandlerByMsgType` 注册了一个消息处理器，当收到匹配的消息类型时自动触发。

**注册参数 — 消息类型 999**：

消息类型（MsgType）是一个整数标识，用于区分不同的消息。999 是自定义消息类型，不在 FISCO BCOS 的预定义范围内，说明这是留给应用层的扩展空间。

**回调参数详解**：

| 参数 | 类型 | 含义 |
|------|------|------|
| `_e` | `NetworkException` | 网络异常对象，`.errorCode() != 0` 表示出错 |
| `_session` | `P2PSession` | P2P 会话对象，代表与某个对等节点的连接 |
| `_msg` | `P2PMessage::Ptr` | 收到的 P2P 消息，包含 payload 和元数据 |

**Echo 处理逻辑**：

```cpp
if (_e.errorCode()) { return; }       // 1. 检查网络异常 → 有异常则丢弃
auto startT = utcTime();              // 2. 记录开始时间
_msg->setRespPacket();                // 3. 设置为响应包
_session->session()->asyncSendMessage(_msg);  // 4. 异步发送回去
```

整个 Echo 逻辑仅 4 步：

1. **错误检查**：如果网络层发生异常（如连接断开），直接返回，不做处理
2. **计时**：用 `utcTime()` 记录处理开始时间
3. **设置响应标志**：`setRespPacket()` 将消息标记为响应包。这是 FISCO BCOS P2P 协议的关键操作——它让接收方知道这是一条响应而不是一条新的请求
4. **异步回发**：`asyncSendMessage` 异步发送，不阻塞当前线程。消息的原内容（payload）原封不动地返回

**日志记录**：

```cpp
BCOS_LOG(INFO) << LOG_DESC("sendResponse")
               << LOG_KV("timeCost", (utcTime() - startT))  // 处理耗时(微秒)
               << LOG_KV("msgSize", (_msg->payload()->size()));  // 消息大小
```

通过 `BCOS_LOG` 宏记录每次 Echo 响应的耗时和消息大小，便于性能监控。

### 第七步：主线程保活（第 72-76 行）

```cpp
    while (true)
    {
        std::this_thread::sleep_for(std::chrono::milliseconds(1));
    }
```

1 毫秒精度的休眠循环。消息处理在 Gateway 内部的线程池中执行，主线程只需要保持进程存活。

## Echo Server 工作原理总结

Echo Server 的数据流可以用一句话概括：

```
客户端发送消息(type=999) → Gateway 接收 → 分发到注册的回调 → setRespPacket() → asyncSendMessage() → 客户端收到原样响应
```

每一步的转换如下：

1. 客户端构造一条 `msgType=999` 的 P2P 消息并发送
2. Echo Server 的 Gateway 收到消息，根据 msgType 找到已注册的回调
3. 回调检查网络异常（无异常则继续）
4. `setRespPacket()` 将消息标记为响应
5. 原封不动地通过 `asyncSendMessage` 发回客户端
6. 客户端收到响应，确认 Echo Server 正常工作

## 设计要点

1. **消息类型注册模式**：通过 `registerHandlerByMsgType` 实现消息分发，类似事件监听器模式。Gateway 负责消息路由，业务逻辑在回调中处理。

2. **异步非阻塞**：`asyncSendMessage` 异步发送，不阻塞消息处理线程，适合高吞吐场景。

3. **配置驱动**：所有网络参数通过 `config.ini` 统一管理，GatewayFactory 自动解析配置并初始化组件。

4. **关注点分离**：Gateway 负责网络连接管理，Service 负责消息分发，回调负责业务逻辑——三层架构清晰。

## 总结

`echo_server_sample.cpp` 虽只有 78 行，但展示了 FISCO BCOS 网络层的核心编程模型：

- **GatewayFactory → Gateway → Service** 的创建链路
- **registerHandlerByMsgType** 的消息类型注册机制
- **setRespPacket + asyncSendMessage** 的 Echo 响应模式
- **config.ini** 驱动的配置管理

理解了这个样例，开发者就能基于 FISCO BCOS 的 P2P 网络层构建自定义的链下通信服务，如自定义协议的监控服务、链下数据同步服务等。
