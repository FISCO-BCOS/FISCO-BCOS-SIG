# 事件订阅样例源码走读

## 前言

在区块链应用中，监听合约事件是一项刚需能力。无论是 DApp 前端需要实时更新 UI、还是后端服务需要同步链上数据，都离不开事件订阅机制。FISCO BCOS 提供了 EventSub（事件订阅）功能，允许客户端按需订阅特定区块范围内特定合约的事件。`eventsub.cpp` 就是展示如何使用 C++ SDK 进行事件订阅的典型案例。本文将逐行解析其源码。

## 整体流程概览

事件订阅的完整流程分为以下步骤：

1. **参数解析** — 读取配置、群组、区块范围、合约地址等参数
2. **SDK 初始化** — 构建并启动 SDK
3. **构建 EventSubParams** — 设置监听范围和过滤条件
4. **注册订阅回调** — 调用 subscribeEvent 注册事件监听
5. **附加区块通知** — 同时注册区块高度通知
6. **主线程等待** — 保持程序运行，持续接收事件

## 分步骤源码解析

### 第一步：usage 函数（第 42-51 行）

```cpp
void usage()
{
    std::cerr << "Desc: subscribe contract events by command params\n";
    std::cerr << "Usage: eventsub <config> <group> <from> <to> <address>[Optional]\n";

    std::cerr << "Example:\n"
              << "    ./eventsub ./config_sample.ini group -1 -1"
                 "\n";
    std::exit(0);
}
```

参数说明：
- **config**：SDK 配置文件路径（如 config_sample.ini）
- **group**：群组 ID
- **from**：起始区块号（整数，-1 表示从最新区块开始）
- **to**：结束区块号（整数，-1 表示持续监听，不设上限）
- **address**：合约地址（可选，不填则监听所有合约的事件）

### 第二步：命令行参数解析（第 55-74 行）

```cpp
    // option params
    std::string config = argv[1];
    std::string group = argv[2];
    int64_t from = atoi(argv[3]);
    int64_t to = atoi(argv[4]);

    std::string address;
    if (argc > 5)
    {
        address = argv[5];
    }
```

前 4 个参数为必选（config、group、from、to），第 5 个参数 address 为可选。`atoi` 将字符串转为整数，`-1` 在 EventSub 中有特殊含义。

**from/to 参数详解**：

| 参数值 | 含义 |
|-------|------|
| `from = -1` | 从当前最新区块开始监听 |
| `from = 100` | 从第 100 个区块开始获取事件 |
| `to = -1` | 持续监听，不设上限（实时监听模式） |
| `to = 200` | 只获取到第 200 个区块为止（历史查询模式） |

最常见的用法是 `from=-1, to=-1`，即实时监听最新事件。

**address 参数详解**：

- **不填 address**：监听全群组所有合约的所有事件
- **填写 address**：仅监听指定合约地址的事件

在实际应用中，通常需要过滤到特定合约，避免收到无关事件。

### 第三步：SDK 初始化（第 76-82 行）

```cpp
    auto factory = std::make_shared<SdkFactory>();
    // construct cpp-sdk object
    auto sdk = factory->buildSdk(config);
    // start sdk
    sdk->start();

    std::cout << LOG_DESC(" [EventSub] start sdk ... ") << std::endl;
```

与所有 SDK 样例一致的三步标准化初始化：创建工厂 → 构建 SDK → 启动服务。

### 第四步：构建 EventSubParams（第 84-91 行）

```cpp
    // construct eventsub params
    auto params = std::make_shared<bcos::cppsdk::event::EventSubParams>();
    params->setFromBlock(from);
    params->setToBlock(to);
    if (!address.empty())
    {
        params->addAddress(address);
    }
```

`EventSubParams` 是事件订阅的参数容器：

- `setFromBlock(from)`：设置起始区块
- `setToBlock(to)`：设置结束区块
- `addAddress(address)`：当 address 不为空时，添加地址过滤条件。可以多次调用 `addAddress` 来订阅多个合约的事件。

### 第五步：区块高度通知注册（第 93-98 行）

```cpp
    sdk->service()->registerBlockNumberNotifier(
        group, [](const std::string& _group, int64_t _blockNumber) {
            // recv block number from server
            std::cout << " \t recv block number notifier from server ===>>>> "
                      << LOG_KV("group", _group)
                      << LOG_KV("blockNumber", _blockNumber) << std::endl;
        });
```

在事件订阅的同时注册了区块高度通知。这是一个**辅助功能**，让用户能观察到链上在持续产生新区块，确认连接正常。注意此处注册的回调与事件订阅是独立的——区块通知和事件通知由不同的通道推送。

### 第六步：事件订阅核心调用（第 101-116 行）

```cpp
    // subscribe event
    sdk->eventSub()->subscribeEvent(
        group, params, [](Error::Ptr _error, const std::string& _events) {
            if (_error)
            {
                // error and exit
                std::cout << " \t something is wrong"
                          << LOG_KV("error", _error->errorCode())
                          << LOG_KV("errorMessage", _error->errorMessage())
                          << std::endl;
                std::exit(-1);
            }
            else
            {
                // recv events from server
                std::cout << " \t recv events from server ===>>>> "
                          << LOG_KV("events", _events)
                          << std::endl;
            }
        });
```

这是整个样例的核心。`subscribeEvent` 的调用分析：

**函数签名**：
```cpp
subscribeEvent(group, EventSubParams, callback)
```

**回调参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| `_error` | `Error::Ptr` | 错误对象，为空或 errorCode=0 表示成功 |
| `_events` | `const std::string&` | 事件数据的 JSON 字符串 |

**回调逻辑**：
1. 如果 `_error` 非空且 errorCode ≠ 0 → 打印错误信息并 **直接退出程序**（`exit(-1)`）
2. 如果成功 → 打印接收到的事件 JSON 数据

注意：`exit(-1)` 意味着事件订阅在遇到错误时会终止整个程序，而非尝试重连。在生产代码中，可以根据业务需求改为重连或降级处理。

**事件 JSON 格式**（示例）：
```json
{
  "events": [
    {
      "address": "0x...",
      "topics": ["0x..."],
      "data": "0x...",
      "blockNumber": "0x100",
      "transactionHash": "0x...",
      "transactionIndex": "0x0",
      "logIndex": "0x0"
    }
  ]
}
```

### 第七步：主线程保持（第 118-124 行）

```cpp
    int i = 0;
    while (true)
    {
        std::cout << LOG_DESC(" Main thread running ") << std::endl;
        std::this_thread::sleep_for(std::chrono::milliseconds(10000));
        i++;
    }
```

事件订阅工作在后台线程，主线程通过无限循环 + 每 10 秒打印心跳的方式保持进程运行，确保订阅连接不中断。

## 总结

`eventsub.cpp` 展示了 FISCO BCOS 事件订阅的完整用法：

1. **参数灵活性**：支持通过 from/to 控制监听范围（历史/实时），通过 address 过滤特定合约
2. **EventSubParams**：作为参数容器，封装了区块范围和地址过滤条件
3. **subscribeEvent 回调**：异步接收事件 JSON，通常由后台推送，无需主动轮询
4. **双重通知**：事件订阅 + 区块通知的组合使用，让开发者同时了解链的区块进度和合约事件

事件订阅是 DApp 开发中连接链上数据和链下应用的关键桥梁。理解这个样例，就掌握了构建响应式区块链应用的基础。
