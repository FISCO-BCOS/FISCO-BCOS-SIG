# FISCO BCOS 部署体验报告

## 一、部署概述

本次体验基于 FISCO BCOS 3.x 官方文档，在 Ubuntu 20.04 环境下使用 `build_chain.sh` 一键建链脚本部署 Air 版 4 节点联盟链。

**部署环境**：
- 操作系统：Ubuntu 20.04 LTS（WSL2 / 云服务器）
- 硬件配置：4 核 CPU、8 GB 内存、50 GB SSD
- FISCO BCOS 版本：v3.11.0
- 部署方式：单机 4 节点 Air 版

## 二、操作步骤

### 2.1 安装依赖

```bash
sudo apt install -y curl openssl wget
```

依赖安装非常轻量，仅需 curl、openssl 和 wget 三个基础工具。相较于 Hyperledger Fabric 需要安装 Docker、Docker Compose、Go、Node.js 等一大堆依赖，FISCO BCOS 的依赖清单极简，**开箱体验很好**。

### 2.2 下载建链脚本

```bash
cd ~ && mkdir -p fisco && cd fisco
curl -#LO https://github.com/FISCO-BCOS/FISCO-BCOS/releases/download/v3.11.0/build_chain.sh
chmod u+x build_chain.sh
```

脚本约 100 KB，下载速度快。同时提供了 Gitee 备用地址，国内用户体验友好。

### 2.3 搭建 4 节点联盟链

```bash
bash build_chain.sh -l 127.0.0.1:4 -p 30300,20200
```

**一条命令，30 秒内完成**：
- 生成 4 个节点的目录结构和配置文件
- 为每个节点生成 RSA 密钥和证书
- 生成 SDK 证书（用于客户端连接）
- 生成创世区块配置

输出日志清晰展示了每一步的执行结果：

```
[INFO] Generate ca cert successfully!
Processing IP:127.0.0.1 Total:4
[INFO] Generate ./nodes/127.0.0.1/sdk cert successful!
[INFO] Generate ./nodes/127.0.0.1/node0/conf cert successful!
[INFO] Generate ./nodes/127.0.0.1/node1/conf cert successful!
[INFO] Generate ./nodes/127.0.0.1/node2/conf cert successful!
[INFO] Generate ./nodes/127.0.0.1/node3/conf cert successful!
[INFO] Admin account: 0x4c7239cfef6d41b7322c1567f082bfc65c69acc5
[INFO] All completed. Files in ./nodes
```

### 2.4 启动区块链

```bash
bash nodes/127.0.0.1/start_all.sh
```

4 个节点秒级启动：

```
node0 start successfully pid=36428
node1 start successfully pid=36433
node2 start successfully pid=36427
node3 start successfully pid=36430
```

### 2.5 部署控制台

控制台是与链交互的命令行工具：

```bash
cd ~/fisco
curl -LO https://github.com/FISCO-BCOS/console/releases/download/v3.6.0/download_console.sh
bash download_console.sh
cp -r nodes/127.0.0.1/sdk/* console/conf
cd console && bash start.sh
```

### 2.6 部署 HelloWorld 合约

在控制台中执行：

```
[group0]: /apps> deploy HelloWorld
transaction hash: 0x...
contract address: 0x...
```

成功部署后，可以调用 get 和 set 方法读写合约数据。

## 三、体验感受

### 优点

1. **部署极简**：从零到运行只需 5 条命令、2 分钟。`build_chain.sh` 一键完成证书生成、节点配置、目录创建等所有繁琐工作，大幅降低了入门门槛。

2. **文档完善**：官方文档覆盖了 Air/Pro/Max 三种架构形态的部署，每个步骤都有详细的命令和预期输出，遇到问题能快速定位。

3. **国密支持**：FISCO BCOS 完整支持国密算法（SM2/SM3/SM4），建链时通过 `-g` 参数即可启用国密模式。对于需要信创合规的企业场景，这是核心优势。

4. **轻量级**：Air 版非常轻量，4 节点部署仅占用约 200 MB 内存，适合在开发机和低配服务器上运行。

5. **资源友好**：多平台支持（Linux/macOS），提供 GitHub 和 Gitee 双下载通道，Docker 方式可选。

6. **工具链齐全**：控制台、区块链浏览器（WeBASE）、监控工具等周边的安装同样便捷，生态完整度高。

### 不足和建议

1. **Windows 原生支持受限**：目前仅支持 Linux 和 macOS 原生部署。Windows 用户需要通过 WSL 或 Docker 间接使用。建议增加 Windows 原生二进制或提供更完善的 Docker Compose 方案。

2. **Docker 镜像拉取速度**：从 Docker Hub 拉取镜像在国内有时较慢，建议同步到阿里云容器镜像服务等国内源。

3. **节点配置文件较多**：`nodes/` 目录下包含大量 ini 格式的配置文件，字段含义对新手不够直观。建议在 `config.ini` 中增加更详细的注释。

4. **控制台交互体验可提升**：控制台是基于 Java 的命令行工具，启动较慢（加载 JVM）。建议考虑提供更轻量的 CLI 替代方案（如基于 Python 的控制台）。

5. **日志文件管理**：长期运行后日志文件会持续增长，建议默认配置日志轮转策略，并在文档中说明日志管理最佳实践。

6. **新手引导可以更友好**：建议提供交互式的向导模式（wizard），引导用户逐步完成第一条链的部署和第一个合约的调用，类似 `npm init` 的交互体验。

## 四、总结

FISCO BCOS 的整体部署体验评分为 **8.5/10**。一键建链脚本将复杂的区块链部署简化为一条命令，极大降低了新手门槛。文档质量、国密支持、生态组件丰富度都是明显的加分项。主要改进空间在于 Windows 兼容性和部分工具的启动性能。

对于有 Linux 使用经验的开发者，FISCO BCOS 的部署体验在同类联盟链项目中处于**领先水平**。
