# 区块高度通知样例源码走读

## 前言

### 什么是区块高度通知

在 FISCO BCOS 区块链网络中，**区块高度（Block Number）** 是标识区块在链上位置的唯一递增序号，每产生一个新的区块，区块高度就会加 1。**区块高度通知** 是一种事件驱动的回调机制，允许应用程序在每次新区块被共识并写入链上时，自动收到通知并执行自定义逻辑。

该机制本质上是一种 **发布-订阅（Pub/Sub）模式** 在区块链 SDK 层的实现。节点作为发布者，在每次出块时发布事件；SDK 作为中间层，负责将事件路由到已注册的回调函数；应用程序作为订阅者，通过回调函数响应事件。

### 应用场景

区块高度通知在以下场景中具有重要价值：

1. **实时交易确认监控**：在收到新区块通知后，可以主动查询该区块中是否包含自己提交的交易，实现更轻量级的交易确认等待机制。
2. **链上数据索引与同步**：每收到一个新区块通知，数据服务层便拉取该区块的完整数据并写入本地数据库，实现链上数据的准实时同步，常用于区块浏览器后端。
3. **事件日志抓取**：结合区块通知与事件订阅，按区块维度批量拉取日志事件，避免轮询带来的资源浪费和延迟。
4. **链状态健康检查**：持续监控区块高度变化，若某段时间内区块高度停滞，则触发告警，说明网络可能出现故障。
5. **合约自动化执行**：定时或按区块间隔触发合约调用，例如每 N 个区块自动执行一次清算或结算逻辑。

---

## 整体流程概览

`blocknotifier.cpp` 是 bcos-cpp-sdk 中演示区块高度通知机制的示例程序，共约 90 行代码。其整体执行流程如下：

```
程序启动
  │
  ├─► usage() 打印参数说明
  │
  ├─► 解析命令行参数 (config, group)
  │
  ├─► 创建 SdkFactory → 构建 SDK 实例 → 启动 SDK
  │
  ├─► 注册 registerBlockNumberNotifier 回调
  │     └─► 每当新区块产生，SDK 自动调用该回调
  │           └─► 回调中打印 "blockNumber: <高度>" 等信息
  │
  └─► 主线程循环：每 10 秒打印 "Main thread running"
        └─► 程序持续运行，直至用户中断 (Ctrl+C)
```

核心设计要点：**回调函数在 SDK 内部的独立线程中执行**，与主线程（每 10 秒打印）互不阻塞，体现了异步、事件驱动的编程模型。

---

## 分步骤源码解析

### 步骤一：usage()——参数说明函数（第 43-51 行）

```cpp
static void usage()
{
    std::cout << "Usage: blocknotifier <config> <group>" << std::endl;
    std::cout << "Example:" << std::endl;
    std::cout << "    ./blocknotifier ./config.ini group" << std::endl;
    std::cout << "Arguments:" << std::endl;
    std::cout << "    config: config file path" << std::endl;
    std::cout << "    group:  group id" << std::endl;
}
```

`usage()` 是一个静态辅助函数，仅在参数不足时被调用。它向用户说明程序的两个必需参数：

- **config**：SDK 配置文件路径。该 INI 格式文件指定了需要连接的区块链节点 IP、端口、链 ID、证书目录等连接参数。
- **group**：群组 ID。FISCO BCOS 支持多群组架构，一个节点可同时参与多个逻辑上隔离的群组，因此必须指定目标群组。

该函数设计简洁，遵循 Unix 命令行工具的习惯用法，帮助用户快速理解程序用法。

---

### 步骤二：main() 函数——程序入口

`main()` 函数是程序的核心入口，位于第 54-90 行，按时间线可以分为四个阶段。

#### 2.1 参数解析阶段（第 56-65 行）

```cpp
if (argc < 3)
{
    usage();
    return -1;
}

std::string config = argv[1];
std::string group = argv[2];
```

程序首先检查参数数量 `argc` 是否小于 3。这里之所以是 3 而非 2，是因为 `argv[0]` 始终是程序自身的可执行文件名，因此实际业务参数需要下标从 1 开始。

若参数不足，程序调用 `usage()` 打印帮助信息后以错误码 -1 退出。若参数充足，则分别将 `argv[1]` 和 `argv[2]` 赋值给 `config` 和 `group` 变量。

#### 2.2 SDK 初始化阶段（第 67-73 行）

```cpp
auto factory = bcos::cppsdk::SdkFactory::build(config);
auto sdk = factory->buildSdk();
sdk->start();
std::cout << "Sdk started, waiting for block notifier..." << std::endl;
```

这是 FISCO BCOS C++ SDK 的标准三段式初始化流程：

| 步骤 | 方法 | 作用 |
|------|------|------|
| 1 | `SdkFactory::build(config)` | 读取配置文件，创建 SDK 工厂实例。工厂负责解析配置并管理 SDK 的构建参数。 |
| 2 | `factory->buildSdk()` | 由工厂构建出具体的 SDK 实例。SDK 实例封装了与节点的 RPC 连接、AMOP 消息通道、事件订阅等核心能力。 |
| 3 | `sdk->start()` | 启动 SDK，建立与区块链节点的网络连接，初始化内部线程池和消息循环。 |

完成以上三步后，SDK 进入正常运行状态，可以接受后续的 API 调用。第 73 行打印一条提示信息，告知用户 SDK 已就绪。

#### 2.3 注册区块高度通知回调（第 75-79 行）——核心逻辑

```cpp
sdk->registerBlockNumberNotifier(group,
    [](const std::string& group, int64_t blockNumber) {
        std::cout << "Received block notifier, group: " << group
                  << ", blockNumber: " << blockNumber << std::endl;
    });
```

这五行代码是整篇示例的 **核心所在**，也是理解区块高度通知机制的关键。下面对其进行逐层分析。

---

## registerBlockNumberNotifier 回调机制深入分析

### 3.1 函数签名与参数语义

`registerBlockNumberNotifier` 是 SDK 实例提供的注册接口，其完整语义为：**为目标群组注册一个"新区块产生"事件的回调函数**。该函数接受两个参数：

| 参数 | 类型 | 含义 |
|------|------|------|
| `group` | `const std::string&` | 目标群组 ID，用于指定监听哪个群组的新区块事件。 |
| `callback` | `std::function<void(const std::string&, int64_t)>` | 回调函数，当新区块产生时被 SDK 自动调用。 |

### 3.2 Lambda 回调的实现

示例使用 C++ Lambda 表达式定义回调函数：

```cpp
[](const std::string& group, int64_t blockNumber) { ... }
```

Lambda 捕获列表为空 `[]`，说明该回调不捕获任何外部变量，仅依赖 SDK 传入的两个参数：

- **group**：`const std::string&` 类型，表示产生新区块的群组 ID。由于同一个 SDK 实例可能同时连接多个群组，此参数帮助回调逻辑区分事件来源。
- **blockNumber**：`int64_t` 类型，表示当前新区块的区块高度。在 FISCO BCOS 中，区块高度从 0 开始严格递增，因此通过该值可以精确判断当前链的同步进度。

### 3.3 回调触发时机与执行模型

回调的触发时机为：**每一次底层区块链网络达成共识并产生新区块**。具体流程如下：

```
区块链节点出块
  └─► 节点通过 RPC 推送通知到 SDK
        └─► SDK 内部 AMOP/RPC 线程接收通知
              └─► SDK 将通知分发给所有已注册的回调
                    └─► 每个回调在 SDK 的工作线程中执行
```

关键点说明：

1. **异步非阻塞**：回调在 SDK 内部的独立工作线程中执行，不会阻塞主线程。这也是为什么示例中需要在 `main()` 末尾加入一个持续运行的循环——否则 `main()` 返回后进程立即退出，回调线程也随之销毁。

2. **线程安全性**：由于回调在独立线程中执行，若回调函数需要访问共享数据，则使用者需要自行保证线程安全（加锁或使用无锁数据结构）。

3. **执行频率**：回调的执行频率与区块链的出块间隔一致。例如，若网络配置的出块间隔为 3 秒，则回调大约每 3 秒被调用一次。

### 3.4 回调内部逻辑

示例中的回调逻辑十分简单：

```cpp
std::cout << "Received block notifier, group: " << group
          << ", blockNumber: " << blockNumber << std::endl;
```

它仅将群组 ID 和新区块高度打印到标准输出。在实际工程中，开发者可以在此处插入自定义业务逻辑，例如：

- 查询该区块内的交易列表；
- 根据交易回执更新本地数据库中的交易状态；
- 推送通知到上层应用（WebSocket、消息队列等）；
- 触发与区块高度相关的智能合约调用。

---

### 步骤三：主线程保活循环（第 81-87 行）

```cpp
while (true)
{
    std::cout << "Main thread running..." << std::endl;
    std::this_thread::sleep_for(std::chrono::seconds(10));
}
```

这是一个无限循环，每隔 10 秒向标准输出打印一次 `"Main thread running..."`。它的作用有两个：

1. **进程保活**：防止 `main()` 函数返回导致进程退出。由于区块通知回调在 SDK 内部线程中执行，主线程必须持续存在以维持进程生命周期。

2. **运行状态可视**：周期性地打印心跳消息，便于运维人员直观地确认程序仍在正常运行。

值得注意的是，主线程使用 `std::this_thread::sleep_for` 进行阻塞等待，而不是忙等待，不会消耗 CPU 资源。

---

## 设计模式与工程启示

从这篇短小精悍的示例中，我们可以提炼出几个值得借鉴的工程实践：

1. **关注点分离**：`usage()` 负责参数说明，`main()` 负责编排流程，回调 Lambda 负责业务响应——职责清晰、各司其职。

2. **Lambda 优于函数指针**：使用 C++11 的 Lambda 表达式定义回调，代码更加紧凑、内聚，避免了传统 C 风格回调需要传递 `void*` 上下文指针的麻烦。

3. **事件驱动架构**：程序不主动轮询区块高度（相比于定时 RPC 查询的开销和延迟），而是通过注册回调实现被动通知——更高效、更实时。

4. **最小化示例**：全文仅约 90 行，删繁就简，聚焦于核心 API `registerBlockNumberNotifier` 的用法演示，是学习 SDK 的最佳入口。

---

## 总结

`blocknotifier.cpp` 是 bcos-cpp-sdk 中演示区块高度通知机制的经典示例，代码量虽小，但完整覆盖了：

- 命令行参数解析与使用引导；
- SDK 工厂模式的标准三段式初始化（`build → buildSdk → start`）；
- `registerBlockNumberNotifier` 回调注册与 Lambda 表达式的实战运用；
- 异步回调的执行模型与线程模型；
- 主线程保活策略。

通过学习该示例，开发者可以快速掌握 FISCO BCOS C++ SDK 中最常用的区块监听模式，并以此为基础构建更复杂的链上数据同步、交易监控和自动化合约执行系统。建议读者在阅读本文后，实际编译运行该示例，观察新区块产生时回调的触发效果，以加深理解。
