# HelloWorld 合约部署源码走读

## 前言

在 FISCO BCOS 中，部署智能合约是区块链应用开发的第一步。`deploy_hello.cpp` 是 bcos-cpp-sdk 中最经典的合约部署示例，它展示了如何使用 C++ SDK 将一段 Solidity 编写的 HelloWorld 合约部署到链上。本文将逐行解析这个样例的源码，帮助开发者理解合约部署的完整流程。

## 整体流程概览

合约部署分为以下几个核心步骤：

1. **配置读取** — 解析命令行参数，获取配置文件路径和群组 ID
2. **SDK 初始化** — 通过 SdkFactory 构建并启动 SDK
3. **群组校验** — 获取 GroupInfo，确认目标群组存在
4. **账户生成** — 根据群组的加密类型（国密/非国密）生成对应的密钥对
5. **获取 BlockLimit** — 获取当前区块高度上限，用于交易生命周期控制
6. **发送部署交易** — 调用 sendTransaction 将合约字节码发送到链上
7. **处理回执** — 在回调中解析 JSON 交易回执，提取合约地址

## 分步骤源码解析

### 第一步：HelloWorld 合约源码（第 35-53 行）

在部署之前，首先需要理解被部署的合约。源码注释中包含了完整的 Solidity 代码：

```solidity
pragma solidity>=0.4.24 <0.6.11;

contract HelloWorld {
    string name;

    constructor() public {
        name = "Hello, World!";
    }

    function get() public view returns (string memory) {
        return name;
    }

    function set(string memory n) public {
        name = n;
    }
}
```

这是一个最简合约：包含一个 `name` 状态变量、一个构造函数（初始化为 "Hello, World!"）、一个 `get()` 读取方法和一个 `set(string)` 写入方法。合约虽小，但完整展示了状态存储和修改的基本模式。

### 第二步：合约字节码（第 54-102 行）

合约部署需要将 Solidity 源码编译为字节码。示例中预编译好了两套字节码：

- **hwBIN**（第 54-77 行）：ECDSA 加密版本的 HelloWorld 合约字节码
- **hwSmBIN**（第 79-102 行）：国密（SM2/SM3）加密版本的 HelloWorld 合约字节码

FISCO BCOS 支持国密和非国密两套加密体系，合约字节码也因此有两种版本。这是 FISCO BCOS 作为国产区块链的重要特性。

### 第三步：ABI 方法签名（第 104-114 行）

```cpp
/*
{
    "6d4ce63c": "get()",
    "4ed3885e": "set(string)"
}
*/
```

这些是合约方法的 Keccak-256 哈希前 4 字节（函数选择器）。当调用合约方法时，SDK 会使用这些选择器来指定调用哪个函数。

### 第四步：getBinary 函数（第 118-121 行）

```cpp
std::string getBinary(bool _sm)
{
    return _sm ? hwSmBIN : hwBIN;
}
```

根据 `_sm`（国密标志）返回对应的字节码。这个简单的三元运算符体现了国密/非国密双轨并行的设计思想。

### 第五步：usage 函数（第 123-131 行）

```cpp
void usage()
{
    std::cerr << "Desc: deploy HelloWorld contract\n";
    std::cerr << "Usage: deploy_hello <config> <groupID>\n"
              << "Example:\n"
              << "    ./deploy_hello ./config_sample.ini group0\n"
                 "\n";
    std::exit(0);
}
```

打印使用说明。示例的命令行参数为 `config_sample.ini`（SDK 配置文件）和 `group0`（群组 ID）。

### 第六步：main 函数 — 参数解析（第 133-145 行）

```cpp
int main(int argc, char** argv)
{
    if (argc < 3)
    {
        usage();
    }

    std::string config = argv[1];
    std::string group = argv[2];

    std::cout << LOG_DESC(" [DeployHello] params ===>>>> ")
              << LOG_KV("\n\t # config", config)
              << LOG_KV("\n\t # groupID", group) << std::endl;
```

程序要求至少 3 个参数（程序名 + config + group）。`LOG_DESC` 和 `LOG_KV` 是 FISCO BCOS 的日志宏，提供统一的日志格式。

### 第七步：SDK 初始化（第 146-152 行）

```cpp
    auto factory = std::make_shared<SdkFactory>();
    // construct cpp-sdk object
    auto sdk = factory->buildSdk(config);
    // start sdk
    sdk->start();

    std::cout << LOG_DESC(" [DeployHello] start sdk ... ") << std::endl;
```

这是整个流程中最关键的三步：
1. **创建 SdkFactory**：SDK 的工厂类，负责根据配置构建 SDK 实例
2. **buildSdk(config)**：读取配置文件（如 config_sample.ini），初始化 WebSocket 连接、线程池、证书等
3. **sdk->start()**：启动 SDK，建立与区块链节点的连接

### 第八步：群组校验（第 154-161 行）

```cpp
    // get group info
    bcos::group::GroupInfo::Ptr groupInfo = sdk->service()->getGroupInfo(group);
    if (!groupInfo)
    {
        std::cout << LOG_DESC(" [DeployHello] group not exist")
                  << LOG_KV("group", group) << std::endl;
        exit(-1);
    }
```

通过 `sdk->service()->getGroupInfo(group)` 获取群组信息。如果返回空指针，说明群组不存在，程序直接退出。这里返回的 `GroupInfo` 包含群组的重要元信息，比如加密类型（`smCryptoType()`）。

### 第九步：账户生成（第 163-167 行）

```cpp
    auto transactionBuilder = std::make_shared<bcos::cppsdk::utilities::TransactionBuilder>();

    auto keyPairBuilder = std::make_shared<KeyPairBuilder>();
    auto keyPair = keyPairBuilder->genKeyPair(
        groupInfo->smCryptoType() ? CryptoType::SM2 : CryptoType::Secp256K1);
```

1. 创建 `TransactionBuilder`：交易构造器，内含两套加密套件
2. 创建 `KeyPairBuilder`：密钥对生成器
3. **根据群组加密类型生成密钥对**：
   - 国密群组 → `CryptoType::SM2`（SM2 椭圆曲线）
   - 非国密群组 → `CryptoType::Secp256K1`（与以太坊兼容的椭圆曲线）

### 第十步：加密套件选择（第 169-171 行）

```cpp
    bcos::crypto::CryptoSuite* cryptoSuite = groupInfo->smCryptoType() ?
                                                 &*transactionBuilder->smCryptoSuite() :
                                                 &*transactionBuilder->ecdsaCryptoSuite();
```

根据群组加密类型选择对应的加密套件。后续用于计算账户地址和签名交易。

### 第十一步：获取 BlockLimit（第 181-185 行）

```cpp
    int64_t blockLimit = -1;
    sdk->service()->getBlockLimit(group, blockLimit);

    std::cout << LOG_DESC(" [DeployHello] block limit ")
              << LOG_KV("blockLimit", blockLimit) << std::endl;
```

BlockLimit 是 FISCO BCOS 交易的生命周期控制机制。交易只能在当前区块号到 blockLimit 之间被处理，超时则失效。这是一种防止交易重放的保护机制。

### 第十二步：构造与发送部署交易（第 187-231 行）

```cpp
    auto hexBin = getBinary(groupInfo->smCryptoType());
    auto binBytes = fromHexString(hexBin);

    auto rpcService = sdk->jsonRpcService();

    std::promise<bool> p;
    auto f = p.get_future();
    rpcService->sendTransaction(*keyPair, group, "", "", std::move(*binBytes),
        "", 0, "extraData",
        [&p](bcos::Error::Ptr _error, std::shared_ptr<bcos::bytes> _resp) {
            // 回调处理...
            p.set_value(true);
        });

    f.get();
```

让我们逐步分析这个核心调用：

**sendTransaction 参数说明**：

| 参数 | 值 | 含义 |
|------|-----|------|
| keyPair | `*keyPair` | 用于签名的密钥对 |
| group | `group` | 目标群组 ID |
| to | `""` | 接收地址（部署合约时为空） |
| input | `""` | ABI 编码的调用数据（部署时为空） |
| data | `binBytes` | 合约字节码（部署交易的核心 payload） |
| abi | `""` | 合约 ABI（部署时为空） |
| blockLimit | `0` | 区块上限（0 表示使用默认值） |
| extraData | `"extraData"` | 附加数据 |
| callback | lambda | 异步回调函数 |

**Promise/Future 同步机制**：

```cpp
std::promise<bool> p;
auto f = p.get_future();
// ... 异步调用 ...
f.get();  // 阻塞等待回调执行完成
```

`sendTransaction` 是异步的，但部署流程需要等待结果。这里使用 C++ 标准库的 `promise/future` 机制，将异步回调转换为同步等待：回调中调用 `p.set_value(true)` 通知 future，主线程在 `f.get()` 处阻塞直到回调完成。

### 第十三步：回调处理 — 解析交易回执（第 195-231 行）

```cpp
[&p](bcos::Error::Ptr _error, std::shared_ptr<bcos::bytes> _resp) {
    if (_error && _error->errorCode() != 0)
    {
        // 错误处理：打印 errorCode 和 errorMessage
        std::cout << LOG_DESC(" [DeployHello] send transaction response error")
                  << LOG_KV("errorCode", _error->errorCode())
                  << LOG_KV("errorMessage", _error->errorMessage()) << std::endl;
    }
    else
    {
        // 成功：将 bytes 转为 string，用 Json::Reader 解析
        std::string receipt = std::string(_resp->begin(), _resp->end());
        Json::Value root;
        Json::Reader jsonReader;

        if (!jsonReader.parse(receipt, root))
        {
            // JSON 解析失败
            std::cout << LOG_DESC(" [DeployHello] [ERROR] recv invalid json object")
                      << LOG_KV("resp", receipt) << std::endl;
            return;
        }

        // 提取合约地址
        std::cout << LOG_DESC(" [DeployHello] contract address ==> " +
                              root["result"]["contractAddress"].asString())
                  << std::endl;
    }
    p.set_value(true);
}
```

回调函数的处理逻辑：

1. **错误检查**：优先判断 `_error` 是否为空且 errorCode 是否为 0
2. **JSON 解析**：将响应 bytes 转为 string，使用 `Json::Reader` 解析
3. **提取 contractAddress**：从 `root["result"]["contractAddress"]` 获取部署后的合约地址——这是部署交易最重要的返回值
4. **异常保护**：整个解析过程包裹在 try-catch 中，防止格式错误导致崩溃

### 第十四步：备选方案 — TransactionBuilderService（第 234-286 行）

源码中以注释形式保留了一种备选的部署方式：

```cpp
/*
auto transactionBuilderService =
    std::make_shared<TransactionBuilderService>(sdk->service(), group, transactionBuilder);

auto r = transactionBuilderService->createSignedTransaction(*keyPair, "", *binBytes.get(), "", 0);
// r.first = tx hash, r.second = signed tx data
sdk->jsonRpc()->sendTransaction(group, "", r.second, false, callback);
*/
```

这种方式将交易构造和签名分离为独立的步骤：先用 `TransactionBuilderService::createSignedTransaction` 手动签名交易，获取交易哈希和签名后的数据，再通过 `jsonRpc()->sendTransaction` 发送。这种方式更灵活，适合需要对交易做更多控制的场景。

对比两种方式：

| 方式 | 特点 | 适用场景 |
|------|------|----------|
| `rpcService->sendTransaction` | 一步完成签名+发送 | 简单部署，推荐入门使用 |
| `TransactionBuilderService` + `jsonRpc()->sendTransaction` | 签名与发送分离 | 需要手动控制签名流程、离线签名等高级场景 |

## 总结

`deploy_hello.cpp` 完整展示了一次合约部署的端到端流程：

1. 通过 `SdkFactory` 初始化 SDK，建立与链的连接
2. 通过 `GroupInfo` 校验群组存在性，并根据群组加密类型选择对应的加密算法
3. 使用 `KeyPairBuilder` 生成密钥对（SM2 或 Secp256K1）
4. 将预编译的合约字节码通过 `sendTransaction` 发送到链上
5. 在回调中解析 JSON 回执，提取合约地址

这个示例虽小，但涵盖了 FISCO BCOS C++ SDK 的几乎所有核心概念：工厂模式初始化、国密支持、异步回调、JSON 解析。理解了这个样例，就掌握了使用 C++ SDK 开发区块链应用的基础。
