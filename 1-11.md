# AMOP 广播消息样例源码走读

## 前言

AMOP（Advanced Messages On-chain Protocol）是 FISCO BCOS 提供的链下消息通信协议，支持三种消息模式：publish（点对点发布）、subscribe（订阅）和 broadcast（广播）。`broadcast.cpp` 演示了 AMOP 的广播模式，本文将逐行解析源码，并重点对比 broadcast 与 publish 两种模式的本质区别。

## 整体流程概览

broadcast 模式的执行流程非常简洁：

1. 解析命令行参数（config + topic + message）
2. 初始化 SDK
3. 进入循环，每 5 秒广播一次消息
4. 调用 `amop()->broadcast()` 发送消息——**无回调，无响应等待**

## 分步骤源码解析

### 第一步：usage 函数（第 39-46 行）

```cpp
void usage()
{
    std::cerr << "Desc: broadcast amop message by command params\n";
    std::cerr << "Usage: broadcast <config> <topic> <message>\n"
              << "Example:\n"
              << "    ./broadcast ./config_sample.ini topic HelloWorld\n";
    std::exit(0);
}
```

参数与 publish 完全一致：config（配置文件）、topic（话题名）、message（消息内容）。但从用法上已经看不出与 publish 的区别——真正的差异在调用接口上。

### 第二步：命令行参数解析（第 51-61 行）

```cpp
    std::string config = argv[1];
    std::string topic = argv[2];
    std::string msg = argv[3];

    std::cout << LOG_DESC(" [AMOP][Broadcast]] params ===>>>> ")
              << LOG_KV("\n\t # config", config)
              << LOG_KV("\n\t # topic", topic)
              << LOG_KV("\n\t # message", msg) << std::endl;
```

与 publish.cpp 的参数解析完全一致。同样也有日志标签 `[AMOP][Broadcast]]` 多了一个方括号的问题。

### 第三步：SDK 初始化（第 63-69 行）

```cpp
    auto factory = std::make_shared<SdkFactory>();
    // construct cpp-sdk object
    auto sdk = factory->buildSdk(config);
    // start sdk
    sdk->start();

    std::cout << LOG_DESC(" [AMOP][Broadcast] start sdk ... ") << std::endl;
```

标准的 SDK 三段式初始化。

### 第四步：broadcast 核心调用（第 71-80 行）

```cpp
    int i = 0;
    while (true)
    {
        std::cout << LOG_DESC(" broadcast message ===>>>> ")
                  << LOG_KV("topic", topic)
                  << LOG_KV("message", msg) << std::endl;

        sdk->amop()->broadcast(topic,
            bytesConstRef((byte*)msg.data(), msg.size()));
        std::this_thread::sleep_for(std::chrono::milliseconds(5000));
        i++;
    }
```

**关键观察**：`broadcast()` 的调用只有 **2 个参数**，没有超时参数，也**没有回调函数**。这是 broadcast 模式最本质的特征——**发送即忘（fire-and-forget）**。

```cpp
sdk->amop()->broadcast(
    topic,                                      // 参数1: 话题名
    bytesConstRef((byte*)msg.data(), msg.size()) // 参数2: 消息数据
);
```

调用后立即返回，不等待任何响应。消息是否被接收、对方如何处理，发布者都不关心。

## Broadcast vs Publish 对比分析

这是本篇教程的核心——从源码角度深入对比两种模式的差异。

### 对比表

| 维度 | publish | broadcast |
|------|---------|-----------|
| **接口签名** | `publish(topic, data, timeout, callback)` | `broadcast(topic, data)` |
| **参数数量** | 4 个 | 2 个 |
| **回调** | 有，可获取每个订阅者的响应 | 无，发送后立即返回 |
| **超时控制** | 支持，第三个参数控制等待时间 | 不适用 |
| **响应处理** | 回调中处理 `WsMessage` 和 `WsSession` | 完全不需要 |
| **通信模式** | 点对点，发后等回 | 一对多，发后即忘 |
| **可靠性** | 可确认消息是否送达 | 不确认，尽力投递 |
| **应用场景** | 业务通知、状态同步、RPC 式调用 | 全网公告、心跳广播、配置下发 |

### 源码对比

**publish 调用**（`publish.cpp` 第 77-103 行）：

```cpp
sdk->amop()->publish(topic,
    bytesConstRef((byte*)msg.data(), msg.size()),
    -1,  // 超时参数
    [](Error::Ptr _error, WsMessage _msg, WsSession _session) {
        // 双层错误检查
        if (_error) { /* 通信层错误 */ }
        if (_msg->status() != 0) { /* 应用层错误 */ }
        // 提取响应内容
    });
```

**broadcast 调用**（`broadcast.cpp` 第 77 行）：

```cpp
sdk->amop()->broadcast(topic,
    bytesConstRef((byte*)msg.data(), msg.size()));
```

仅两行，干净利落，没有任何响应处理逻辑。

### 何时使用 Broadcast？

broadcast 模式适用于以下场景：

1. **全网公告**：如链升级通知、系统维护公告，不需要确认每个节点都收到
2. **心跳广播**：节点定期广播自己的存活性，不关心响应
3. **配置下发**：将最新配置广播给所有节点，接收方自行决定是否应用
4. **日志/监控数据推送**：推送统计数据，允许丢失个别数据包

### 何时使用 Publish？

publish 模式适用于需要确认和响应的场景：

1. **业务通知**：如交易完成通知，需要确认对方收到
2. **请求-响应模式**：如查询对方状态并获取返回值
3. **对账/清算**：需要对方返回确认或处理结果

## 总结

`broadcast.cpp` 的源码虽短（仅 83 行），但它精准地展示了 AMOP broadcast 模式的本质：

1. **极简接口**：只需 topic 和 data 两个参数，调用即完成
2. **无状态**：发送后立即返回，不关心投递结果
3. **一对多**：广播给所有订阅该 topic 的节点

broadcast 与 publish 的核心区别在于：**publish 期待响应（request-response），broadcast 不关心响应（fire-and-forget）**。选择哪种模式取决于业务对可靠性和确认机制的需求。

理解这对 API 的差异，可以帮助开发者在设计机构间通信方案时做出正确的选择。
